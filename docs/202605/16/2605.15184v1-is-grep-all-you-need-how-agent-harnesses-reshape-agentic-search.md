---
title: Is Grep All You Need? How Agent Harnesses Reshape Agentic Search
title_zh: Grep 是否就是你所需的一切？智能体测试框架如何重塑智能体搜索
authors: "Sahil Sen, Akhil Kasturi, Elias Lumer, Anmol Gulati, Vamse Kumar Subbiah"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15184v1"
tags: ["query:rag"]
score: 8.0
evidence: 对智能体RAG系统中检索策略选择的系统性比较
tldr: 本研究系统探讨了LLM智能体在搜索任务中检索策略与架构的相互作用。通过对比grep（文本匹配）与向量检索，并结合Chronos、Claude Code等多种智能体框架及工具调用模式，研究分析了工具输出呈现形式和干扰信息对性能的影响。实验证明，在长记忆评估任务中，grep往往优于向量检索，且智能体框架的设计对最终表现具有决定性作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15184v1/fig-001.webp\", \"caption\": \"Table 3: Experiment 2 (vector-only): accuracy (%) by session-limit configuration.\", \"page\": 7, \"index\": 1, \"width\": 456, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.15184v1/fig-002.webp\", \"caption\": \"Table 2: Experiment 2 (grep-only): overall accuracy (%) on the 116-question subset by session-limit configuration.\", \"page\": 7, \"index\": 2, \"width\": 494, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.15184v1/fig-003.webp\", \"caption\": \"Table 1: Experiment 1. Overall accuracy (%) on the 116-question LongMemEval-S subset. We report results for both the standard and programmatic tool-calling configurations\", \"page\": 5, \"index\": 3, \"width\": 622, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2605.15184v1/fig-004.webp\", \"caption\": \"Figure 1: Mean performance as noise is added to the retrieval pool. Both methods face minimal degradation, but GREP outperforms vector on average.\", \"page\": 6, \"index\": 4, \"width\": 1056, \"height\": 575}]"
motivation: 现有研究缺乏对检索策略、智能体架构及工具调用范式在智能体搜索中相互作用的系统性比较。
method: 通过两个实验对比了grep与向量检索在不同智能体框架下的表现，并评估了增加无关上下文对检索准确性的影响。
result: 实验发现grep检索在多数情况下准确率高于向量检索，但整体性能表现仍高度依赖于具体的智能体框架和工具调用风格。
conclusion: 检索策略的选择固然重要，但智能体框架的设计和工具结果的呈现方式同样是决定智能体搜索效能的关键因素。
---

## 摘要
大语言模型（LLM）智能体的最新进展实现了复杂的智能体工作流，模型可以自主检索信息、调用工具并对大规模语料库进行推理，从而代表用户完成任务。尽管检索增强生成（RAG）在智能体搜索系统中的应用日益广泛，但现有文献仍缺乏对检索策略选择如何与智能体架构及工具调用范式相互作用的系统性比较。一些重要的实践维度在智能体循环中仍未得到充分探索，包括工具输出如何呈现给模型，以及当搜索必须应对更多无关上下文文本时性能如何变化。本文报告了一项分为两个实验的实证研究。实验 1 在来自 LongMemEval 的 116 个问题样本上比较了 grep 和向量检索，使用了自定义智能体测试框架（Chronos）和厂商原生的 CLI 测试框架（Claude Code、Codex 和 Gemini CLI），涵盖了内联工具结果和模型单独读取的基于文件的工具结果。实验 2 比较了仅使用 grep 和仅使用向量的检索，同时逐渐混入额外的无关对话历史，使每个查询都嵌入在重要段落之外的更多干扰材料中。在 Chronos 和厂商 CLI 中，实验 1 的对比结果显示 grep 通常比向量检索具有更高的准确率；同时，即使底层对话数据相同，总体评分仍强烈取决于所使用的测试框架和工具调用风格。

## Abstract
Recent advances in Large Language Model (LLM) agents have enabled complex agentic workflows where models autonomously retrieve information, call tools, and reason over large corpora to complete tasks on behalf of users. Despite the growing adoption of retrieval-augmented generation (RAG) in agentic search systems, existing literature lacks a systematic comparison of how retrieval strategy choice interacts with agent architecture and tool-calling paradigm. Important practical dimensions, including how tool outputs are presented to the model and how performance changes when searches must cope with more irrelevant surrounding text, remain under-explored in agent loops. This paper reports an empirical study organized into two experiments. Experiment 1 compares grep and vector retrieval on a 116-question sample from LongMemEval, using a custom agent harness (Chronos) and provider-native CLI harnesses (Claude Code, Codex, and Gemini CLI), for both inline tool results and file-based tool results that the model reads separately. Experiment 2 compares grep-only and vector-only retrieval while progressively mixing in additional unrelated conversation history, so that each query is embedded in more distracting material alongside the passages that matter. Across Chronos and the provider CLIs, grep generally yields higher accuracy than vector retrieval in our comparisons in experiment 1; at the same time, overall scores still depend strongly on which harness and tool-calling style is used, even when the underlying conversation data are the same.