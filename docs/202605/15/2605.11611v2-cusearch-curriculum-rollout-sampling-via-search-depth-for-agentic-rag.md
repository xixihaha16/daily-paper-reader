---
title: "CuSearch: Curriculum Rollout Sampling via Search Depth for Agentic RAG"
title_zh: CuSearch：基于搜索深度的智能体 RAG 课程展开采样
authors: "Jianghan Shen, Siqi Luo, Xinyu Cheng, Jing Xiong, Yue Li, Jiyao Liu, Jiashi Lin, Yirong Chen, Junjun He"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11611v2"
tags: ["query:rag"]
score: 10.0
evidence: 具有可验证奖励的强化学习用于智能体RAG
tldr: 本研究针对Agentic RAG在强化学习训练中均匀采样导致效率低下的问题，提出了CuSearch框架。该框架基于搜索深度贪婪分配（SDGA）策略，将有限的更新预算优先分配给搜索深度更深、包含更多检索决策点的轨迹。通过SDGA-Auto和SDGA-Phase两种模式，CuSearch实现了与训练进程同步的课程学习。实验证明，该方法在ZeroSearch等任务上显著优于标准GRPO算法，最高提升11.8分，证明了搜索深度是提升检索策略监督密度的有效代理指标。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11611v2/fig-001.webp\", \"caption\": \"Figure 1: (a) Average search count per trajectory increases over training under answercorrectness reward alone. (b) The within-batch depth distribution shifts toward higher values as training advances.\", \"page\": 2, \"index\": 1, \"width\": 815, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2605.11611v2/fig-002.webp\", \"caption\": \"Table 1: Main results under ZeroSearch (simulator evaluation) and Search-R1 frameworks (EM, %). GRPO-Full and GRPO-Half correspond to the standard training protocol of each framework without trajectory selection. All SDGA variants use Smax = 5. Bold denotes the best result per model group.\", \"page\": 7, \"index\": 2, \"width\": 830, \"height\": 569}, {\"url\": \"assets/figures/arxiv/2605.11611v2/fig-003.webp\", \"caption\": \"Table 4: Performance under different Smax settings (Qwen2.5-3B, ZeroSearch simulator evaluation, EM, %).\", \"page\": 18, \"index\": 3, \"width\": 826, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2605.11611v2/fig-004.webp\", \"caption\": \"Table 3: Ablation on trajectory selection signals under the ZeroSearch framework (simulator evaluation, Qwen2.5-3B, EM, %). Random and Top-K Reward share the same rollout generation and update cost as CuSearch.\", \"page\": 18, \"index\": 4, \"width\": 772, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2605.11611v2/fig-005.webp\", \"caption\": \"Figure 4: Training template. The question is appended to the end during training and inference.\", \"page\": 13, \"index\": 5, \"width\": 814, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2605.11611v2/fig-006.webp\", \"caption\": \"Figure 3: Training dynamics on ZeroSearch with Qwen2.5-3B. Panels (a–c) show EM on NQ, mean reward, and average search depth per rollout. SDGA-Phase achieves the highest values on all three metrics by the end of training.\", \"page\": 8, \"index\": 6, \"width\": 821, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2605.11611v2/fig-007.webp\", \"caption\": \"Figure 2: Overview of CuSearch. Top: A query generates N ·G rollouts. SDGA then selects K by search depth for gradient updates. Bottom left: SDGA greedily allocates budget K across depth buckets by priority and capacity. Bottom right: SDGA-Auto always targets the deepest available bucket, with selections advancing deeper as the depth distribution shifts upward during training. SDGA-Phase explicitly advances the target bucket as deeper trajectories become sufficiently abundant.\", \"page\": 4, \"index\": 7, \"width\": 798, \"height\": 441}]"
motivation: 传统的强化学习训练方法对所有轨迹一视同仁，忽略了深层搜索轨迹含有更丰富的检索决策信息和更强的监督信号。
method: 提出CuSearch框架，利用SDGA算子根据轨迹的搜索深度动态调整采样权重，使模型优先学习复杂度更高的检索行为。
result: 实验结果显示CuSearch在多种模型上均有显著提升，在ZeroSearch基准测试中比标准GRPO算法高出11.8个百分点。
conclusion: 搜索深度是Agentic RAG训练中一种无需人工标注且可靠的监督密度代理指标，能显著增强强化学习的训练效果。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为从仅结果监督中训练智能体检索增强生成（RAG）系统的一种极具前景的范式。大多数现有方法通过均匀采样的展开（rollouts）来优化策略，隐含地将所有轨迹视为具有同等的信息量。然而，轨迹在搜索深度上存在显著差异，因此信息量并不相等：搜索深度较深的轨迹包含更多的检索决策点，并为检索子策略提供更密集的直接监督。此外，随着训练的进行，这种异质性会不断增加，因为批次内的深度分布会向更高值偏移，但均匀展开采样却忽视了这种偏移。为了解决这个问题，我们提出了 CuSearch，这是一个基于搜索深度贪婪分配（SDGA）的课程展开采样框架。SDGA 是一种批次级算子，它将固定的更新预算重新分配给搜索深度更深的轨迹。SDGA-Auto 始终针对当前批次中最深的可用轨迹，随着深度分布向上偏移，产生一种隐含的与训练对齐的课程。SDGA-Phase 则在深层轨迹变得足够丰富时，显式地推进课程阈值。在不同模型类型和检索框架上的实验表明，CuSearch 持续提升了性能，在 ZeroSearch 上比标准 GRPO 提高了多达 11.8 个精确匹配点。这些结果表明，在基于 RLVR 的智能体 RAG 训练中，单条轨迹的搜索深度是检索监督密度的一种可靠且无需标注的代理指标。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a promising paradigm for training agentic retrieval-augmented generation (RAG) systems from outcome-only supervision. Most existing methods optimize policies from uniformly sampled rollouts, implicitly treating all trajectories as equally informative. However, trajectories differ substantially in search depth and are therefore not equally informative: deeper-search trajectories contain more retrieval decision points and provide denser direct supervision for the retrieval sub-policy. Moreover, this heterogeneity grows over training as the within-batch depth distribution shifts toward higher values, yet uniform rollout sampling remains blind to this shift. To address this, we propose CuSearch, a curriculum rollout sampling framework built on Search-Depth Greedy Allocation (SDGA), a batch-level operator that reallocates a fixed update budget toward deeper-search trajectories. SDGA-Auto always targets the deepest available trajectories in the current batch, yielding an implicit training-aligned curriculum as the depth distribution shifts upward. SDGA-Phase explicitly advances the curriculum threshold as deeper trajectories become sufficiently abundant. Experiments across model types and retrieval frameworks show that CuSearch consistently improves performance, achieving up to 11.8 exact-match points over standard GRPO on ZeroSearch. These results establish per-trajectory search depth as a reliable, annotation-free proxy for retrieval supervision density in RLVR-based agentic RAG training.

---

## 论文详细总结（自动生成）

### CuSearch: 基于搜索深度的智能体 RAG 课程展开采样论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在智能体检索增强生成（Agentic RAG）的训练中，通常使用具有可验证奖励的强化学习（RLVR，如 GRPO）来优化模型。现有的方法通常对所有采样的轨迹（rollouts）进行均匀采样和同等权重的更新。
*   **研究动机**：作者发现，并非所有轨迹的信息量都是等价的。在同一批次中，搜索深度（即检索调用次数）较深的轨迹包含更多的检索决策点，能为检索子策略提供更密集的监督信号。
*   **核心问题**：随着训练进行，模型生成的轨迹深度分布会发生偏移，传统的均匀采样会稀释深层轨迹的贡献，导致训练效率低下。论文旨在探索如何利用“搜索深度”这一无需标注的结构化信号来优化采样策略。

#### 2. 方法论
论文提出了 **CuSearch** 框架，其核心是 **SDGA（搜索深度贪婪分配）** 算子。
*   **核心思想**：在固定的更新预算（K 条轨迹）下，优先选择搜索深度更深的轨迹参与梯度计算，而非随机选择或仅按奖励高低选择。
*   **关键技术细节**：
    *   **分桶机制**：将所有生成的轨迹按搜索次数（0 到 $S_{max}$）放入不同的桶中。
    *   **SDGA-Auto（自动模式）**：一种无状态的贪婪策略，始终优先填满深度最深的桶。随着模型能力的提升，深层轨迹自然增多，形成一种隐含的、与训练进度同步的课程。
    *   **SDGA-Phase（阶段模式）**：显式的课程学习。设定一个深度阈值，只有当深层轨迹在采样池中达到足够比例时，才不可逆地推进到下一个深度阶段。
*   **算法流程**：在每一步训练中，先生成 $N \times G$ 条轨迹，通过 SDGA 算子筛选出 $K$ 条最具代表性的深层轨迹，仅对这 $K$ 条轨迹计算优势函数（Advantage）并更新策略网络。

#### 3. 实验设计
*   **数据集/场景**：涵盖了 7 个开放域问答基准测试，包括单跳（NQ, TriviaQA, PopQA）和多跳（HotpotQA, 2WikiMultiHopQA, MuSiQue, Bamboogle）任务。
*   **检索框架**：
    1.  **ZeroSearch**：使用 LLM 模拟的检索环境（高随机性）。
    2.  **Search-R1**：使用基于分块的离线检索语料库（低随机性）。
*   **对比方法（Baselines）**：
    *   **GRPO-Full/Half**：标准 GRPO 算法。
    *   **Random**：随机选择轨迹（验证计算量公平性）。
    *   **Top-K Reward**：按奖励得分筛选轨迹（验证深度信号的独特性）。
    *   **DAPO / AR3PO**：现有的提示词级（Prompt-level）自适应采样方法。

#### 4. 资源与算力
*   **硬件环境**：实验在 **8 × NVIDIA A100 80G GPU** 集群上完成。
*   **模型规模**：使用了 Qwen2.5 (3B, 7B) 和 LLaMA-3.2-3B-Base。
*   **训练参数**：学习率 $1 \times 10^{-6}$，KL 惩罚系数 $\beta=0.001$，每步采样 192 条轨迹（$N=64, G=3$），更新预算 $K=96$。
*   **说明**：文中未明确给出总训练时长（小时/天），但提供了训练步数（约 200 步）作为参考。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了跨模型（3 种）、跨框架（2 种）、跨任务（7 个数据集）的广泛测试。
*   **消融实验**：设计了 6 组深入的消融实验，包括信号方向性（SDGA-Anti）、对 $S_{max}$ 的鲁棒性、真实搜索 API 的迁移性等。
*   **充分性评价**：实验设计非常充分且严谨。通过与“随机采样”和“按奖励采样”的对比，排除了“仅因更新量减少”或“仅因奖励更高”导致性能提升的可能性，客观地证明了搜索深度作为监督密度代理指标的有效性。

#### 6. 主要结论与发现
*   **性能显著提升**：在 ZeroSearch 框架下，CuSearch 相比标准 GRPO 在 Qwen2.5-3B 上提升了 **11.8** 个 EM 百分点。
*   **深度即信息**：搜索深度是检索监督密度的可靠代理。优先训练深层轨迹能显著加速检索策略的学习。
*   **环境影响**：检索环境的随机性（Stochasticity）是增益的放大器。在模拟检索或真实 Web 搜索中，CuSearch 的表现优于静态的离线分块检索。
*   **课程价值**：显式的阶段性课程（SDGA-Phase）在训练初期比自动模式更稳定，能避免因深层轨迹稀缺导致的采样退化。

#### 7. 优点
*   **无需额外标注**：不需要过程监督（Process Supervision）或昂贵的专家轨迹，仅利用轨迹自身的结构特征。
*   **即插即用**：作为采样层嵌入，不改变原有的奖励函数或模型架构，具有很强的通用性。
*   **理论支撑**：附录中通过梯度覆盖范围（Gradient Coverage）的推导，为为何最大化搜索深度能优化检索策略提供了数学解释。

#### 8. 不足与局限
*   **环境依赖性**：在搜索深度增长缓慢或检索环境过于确定（如重复查询返回完全相同内容）的情况下，该方法的增益会收窄。
*   **单一特征**：目前仅使用搜索深度作为标量特征，未考虑搜索查询的质量或检索结果的相关性。
*   **任务局限**：主要针对 RAG 任务，尚未验证其在工具调用（Tool Use）或代码执行等其他智能体行为中的泛化能力。

（完）
