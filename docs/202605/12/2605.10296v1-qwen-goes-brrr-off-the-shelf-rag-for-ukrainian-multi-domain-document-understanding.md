---
title: "Qwen Goes Brrr: Off-the-Shelf RAG for Ukrainian Multi-Domain Document Understanding"
title_zh: Qwen Goes Brrr：用于乌克兰语多领域文档理解的开箱即用 RAG
authors: "Anton Bazdyrev, Ivan Bashtovyi, Ivan Havlytskyi, Oleksandr Kharytonov, Artur Khodakovskyi"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10296v1"
tags: ["query:rag"]
score: 9.0
evidence: 包含问题感知密集检索和重排序的检索增强流水线
tldr: 本研究针对第五届UNLP乌克兰语多领域文档理解任务，提出了一种基于检索增强生成（RAG）的流水线。该系统核心包含PDF上下文分块、感知问题与选项的稠密检索与重排序，以及受限答案生成。通过使用Qwen3系列模型，系统在保持文档结构的同时，显著提升了检索召回率和答案准确性，在私有排行榜上取得了0.9598的优异成绩，证明了该方法的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10296v1/fig-001.webp\", \"caption\": \"Table 3: Reranker comparison with fixed off-the-shelf Qwen3-Embedding-8B retriever and Qwen3-32B-AWQ generator. The Private leaderboard evaluates on an unseen third domain.\", \"page\": 5, \"index\": 1, \"width\": 460, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2605.10296v1/fig-002.webp\", \"caption\": \"Table 2: Embedding comparison with fixed off-theshelf Qwen3-Reranker-8B and Qwen3-32B-AWQ generator (Yang et al., 2025). 80k denotes our machinetranslated auxiliary retrieval corpus.\", \"page\": 5, \"index\": 2, \"width\": 475, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2605.10296v1/fig-003.webp\", \"caption\": \"Table 4: Leaderboard scores for key variants. Ablation rows use Qwen3-Embedding-8B, Qwen3-Reranker8B, top-20→2 selection, full Q+A at both retrieval and reranking, and Qwen3-32B-AWQ, unless otherwise noted.\", \"page\": 6, \"index\": 3, \"width\": 467, \"height\": 318}]"
motivation: 旨在解决乌克兰语多领域PDF文档中的多项选择题解答及支撑文档定位问题。
method: 采用Qwen3系列模型构建RAG流水线，包含上下文分块、感知选项的重排序及基于Top-2段落的受限生成。
result: "重排序将Recall@1提升至0.7935，最终系统在私有排行榜上达到了0.9598的高分。"
conclusion: 在严格的竞赛约束下，保留文档结构并使相关性评估感知答案空间比复杂的启发式方法更有效。
---

## 摘要
我们参加了第五届 UNLP 多领域文档理解共享任务，该任务要求系统根据 PDF 集合回答乌克兰语多项选择题，并定位支持文档及其页码。我们提出了一个围绕三个核心理念构建的检索增强流水线：PDF 的上下文分块、基于问题和备选项感知型的稠密检索与重排序，以及基于少量重排序段落的受限答案生成。我们的最终系统使用 Qwen3-Embedding-8B 进行检索，使用微调后的 Qwen3-Reranker-8B 进行段落排序，并使用 Qwen3-32B 进行答案选择。在留出集上，重排序将 Recall@1 从 0.6957 提升至 0.7935，而使用前两个重排序段落将答案准确率从 0.9348 提升至 0.9674。我们在排行榜上的最佳成绩在公开榜单上达到 0.9452，在私有榜单上达到 0.9598。我们的结果表明，在严格的代码竞赛限制下，保留文档结构并使相关性评估感知答案空间，比添加复杂的下游启发式方法更为有效。

## Abstract
We participated in the Fifth UNLP shared task on multi-domain document understanding, where systems must answer Ukrainian multiple-choice questions from PDF collections and localize the supporting document and page. We propose a retrieval-augmented pipeline built around three ideas: contextual chunking of PDFs, question-aware dense retrieval and reranking conditioned on both the question and answer options, and constrained answer generation from a small set of reranked passages. Our final system uses Qwen3-Embedding-8B for retrieval, a fine-tuned Qwen3-Reranker-8B for passage ranking, and Qwen3-32B for answer selection. On a held-out split, reranking improves Recall@1 from 0.6957 to 0.7935, while using the top-2 reranked passages raises answer accuracy from 0.9348 to 0.9674. Our best leaderboard run reached 0.9452 on the public leaderboard and 0.9598 on the private leaderboard. Our results suggest that, under strict code-competition constraints, preserving document structure and making relevance estimation aware of the answer space are more effective than adding complex downstream heuristics.

---

## 论文详细总结（自动生成）

这篇论文介绍了参加第五届 UNLP 共享任务的系统方案，重点解决乌克兰语多领域文档理解中的多项选择题解答及证据定位问题。

### 1. 核心问题与整体含义
*   **研究动机：** 现实世界的文档理解不仅需要从段落中提取答案，还需要在长篇、异构且具有特定领域特征的 PDF 文档中导航，定位证据并将其与具体决策（如多选题选项）挂钩。
*   **背景：** 任务要求在严格的竞赛限制下（Kaggle 环境、无网络、有限算力和时间），针对乌克兰语这一相对低资源语言，实现高精度的答案预测、文档识别和页码定位。

### 2. 方法论
该系统构建了一个多阶段的检索增强生成（RAG）流水线，核心思想是“保留结构”和“感知选项”：
*   **上下文分块（Contextual Chunking）：** 不将 PDF 视为纯文本，而是利用 `pymupdf4llm` 转换为 Markdown。每个分块（512 token）都包含三层嵌套上下文：文档开头的全局前缀（128 token）、当前章节的标题路径（层级信息）以及局部正文。
*   **问题与选项感知的检索：** 使用 **Qwen3-Embedding-8B**。检索查询不仅包含问题，还包含所有 6 个备选项和引导指令，以增强检索的针对性。
*   **选项感知的重排序（Reranking）：** 使用微调后的 **Qwen3-Reranker-8B**。重排序器同时接收问题和所有选项，这对于区分那些看似相关但不支持正确选项的干扰段落至关重要。
*   **受限答案生成：** 使用 **Qwen3-32B-AWQ** 量化模型。将重排序后的 Top-2 段落输入模型，并限制其仅输出单个 token（A-F）。支持文档和页码信息直接从排名最高的段落元数据中提取。

### 3. 实验设计
*   **数据集：** 
    *   官方数据：40 个训练问题，461 个开发问题（涵盖 2 个可见领域）。
    *   辅助数据：**UA-SQuAD**（乌克兰语抽取式 QA）以及作者构建的 **80k 乌克兰语检索预训练语料**（通过 TranslateGemma-27B 将 NQ、HotpotQA 等英文数据集翻译而来）。
*   **Benchmark 与对比方法：**
    *   对比了不同规模的 Qwen3 模型（0.6B, 4B, 8B, 14B, 32B）。
    *   对比了多种嵌入模型（如 `pplx-embed`）。
    *   消融实验：测试了无文档上下文前缀、使用摘要代替前缀、仅使用问题检索（不带选项）等变体。
    *   探索了 Agent 模式（工具轮询）和领域分类器路由，但最终因鲁棒性或效率问题未采用。

### 4. 资源与算力
*   **硬件环境：** 竞赛在 Kaggle 平台上运行，配置为 **2× Tesla T4 GPU**。
*   **运行限制：** 整个推理流水线必须在 9 小时内完成。
*   **训练细节：** 文中提到对重排序器和嵌入模型进行了微调，但未详细说明训练阶段的具体总时长，仅强调推理阶段必须适配 T4 显卡（不支持 BF16，因此使用了 FP16 和 AWQ 量化）。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了多组详尽的对比实验，包括 8 种嵌入模型配置对比、11 种重排序模型配置对比以及 6 组核心流水线消融实验。
*   **充分性：** 实验设计较为客观，通过公开榜单（Public）和包含未见领域的私有榜单（Private）双重验证，有效评估了系统的泛化能力。消融实验清晰地展示了每个组件（尤其是上下文分块）的贡献。

### 6. 主要结论与发现
*   **结构信息至关重要：** 在分块中添加文档前缀和标题路径是提升性能的关键，移除前缀会导致得分大幅下降（0.945 -> 0.861）。
*   **选项感知提升精度：** 让检索和重排序阶段“看到”备选项，能显著增强模型区分干扰项的能力。
*   **模型规模与微调：** 较大的预训练模型（如 8B 嵌入/重排序器）在开箱即用时表现强劲，而较小模型通过高质量辅助数据微调可以缩小差距。
*   **简单即是美：** 在严格约束下，精心设计的线性 RAG 流水线比复杂的 Agent 逻辑或硬性的领域路由更稳健。

### 7. 优点
*   **实用性强：** 方案针对工业级/竞赛级约束进行了深度优化，具有很强的落地参考价值。
*   **资源贡献：** 发布了首个大规模乌克兰语检索预训练数据集（80k silver data），填补了该语言在检索领域的空白。
*   **结构化处理：** 提出了一种简单有效的 PDF 结构保留方法，解决了长文档定位难的问题。

### 8. 不足与局限
*   **翻译偏差：** 辅助预训练数据主要依靠机器翻译，可能存在翻译腔或语义细微偏差，且未经过人工校验。
*   **硬件局限：** 实验主要基于较旧的 T4 GPU，未能在更现代的硬件（支持 BF16）上测试更高性能的模型变体。
*   **领域泛化：** 虽然在私有榜单表现优异，但对于完全脱离现有 PDF 格式规范的极端新领域，其基于 Markdown 的解析策略可能仍有失效风险。

（完）
