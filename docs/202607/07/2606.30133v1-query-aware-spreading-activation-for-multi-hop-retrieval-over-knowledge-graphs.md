---
title: Query-Aware Spreading Activation for Multi-Hop Retrieval over Knowledge Graphs
title_zh: 知识图谱上多跳检索的查询感知激活扩散方法
authors: "Illia Makarov, Mykola Glybovets"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30133v1"
tags: ["query:rag"]
score: 8.5
evidence: Graph RAG的查询感知检索
tldr: 本文针对知识图谱多跳检索中遍历过程忽略查询语义的问题，提出一种查询感知的激活扩散方法。通过引入语义门控计算实体与问题的相似度，实现了高效的查询引导遍历。该方法可作为单个Cypher查询在图数据库内直接执行，在MuSiQue数据集上取得了与最先进方法相当的准确率，并显著降低了检索延迟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30133v1/fig-001.webp\", \"caption\": \"TABLE II KNOWLEDGE-GRAPH STATISTICS FOR EACH BENCHMARK.\", \"page\": 7, \"index\": 1, \"width\": 1074, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2606.30133v1/fig-002.webp\", \"caption\": \"TABLE I THE PROPOSED METHOD AMONG REPRESENTATIVE GRAPH RAG SYSTEMS.\", \"page\": 3, \"index\": 2, \"width\": 1002, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.30133v1/fig-003.webp\", \"caption\": \"Fig. 1. Two-phase architecture. The indexing phase (top) extracts entities and relations from the corpus via an LLM, resolves and embeds them, and stores the result as a typed knowledge graph in Neo4j. The query phase (bottom) uses an LLM to map the question to seed nodes, runs spreading activation for T iterations in a single Cypher query, and passes the activated subgraph to an LLM to generate the answer.\", \"page\": 4, \"index\": 3, \"width\": 1076, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2606.30133v1/fig-004.webp\", \"caption\": \"Fig. 2. Knowledge-graph schema. Document and paragraph nodes preserve the original structure of the corpus; :MENTIONS edges record entity mentions at two levels: the document (aggregated) and the paragraph (with exact location). Entities and their typed binary relations form the basis for multi-hop reasoning. The self-loop denotes the :RELATES_TO edge, which connects two entities and stores the relation label as a property.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2606.30133v1/fig-005.webp\", \"caption\": \"TABLE V COMPONENT ABLATION OVER ELEVEN CONFIGURATIONS (n = 500).\", \"page\": 9, \"index\": 5, \"width\": 1052, \"height\": 776}, {\"url\": \"assets/figures/arxiv/2606.30133v1/fig-006.webp\", \"caption\": \"Fig. 3. Sensitivity analysis over the propagation depth (left) and the decay factor (right) at n = 500 per dataset. Stars mark the default configuration T = 3, α = 0.7. The dashed horizontal lines show the naive-vector baseline F1 on each dataset.\", \"page\": 9, \"index\": 6, \"width\": 1076, \"height\": 460}]"
motivation: 现有的图检索系统在多跳遍历时往往忽略了查询语义，且复杂的迭代求解器难以与图数据库高效集成。
method: 提出一种基于语义门控的激活扩散方法，通过计算候选实体描述与问题的相似度来控制传播权重，并将其实现为单次Cypher查询。
result: 在MuSiQue数据集上，该方法性能与QAFD-RAG相当，且在F1得分和检索延迟上显著优于HippoRAG等基准模型。
conclusion: 引入查询感知的语义门控不仅能显著提升多跳检索的准确性，还能通过简化计算流程大幅降低系统延迟。
---

## 摘要
基于知识图谱的检索增强生成（Graph RAG）通过利用图结构，在多跳问答任务上优于扁平的文本段落检索。然而，在大多数现有系统中，问题仅用于设定种子节点；随后的遍历过程变得“查询盲目”，完全依赖于图结构。QAFD-RAG 是一个例外，它通过结合边重加权的流扩散求解器实现了查询感知遍历。这种架构需要将整个图加载到 Python 内存中，且迭代求解器的迭代次数不固定，这使得与图数据库的集成变得复杂。我们提出了一种激活扩散方法，通过单一的每步语义门控实现了相同的查询感知遍历：步长权重是候选实体描述与问题之间的余弦相似度，且迭代次数固定。整个检索过程——种子映射、传播、Top-K 选择和上下文组装——被表达为单个 Cypher 查询，并在与 Neo4j 的一次往返中执行；图数据从未离开数据库。在 MuSiQue 数据集上，我们的方法在精确匹配（EM）上与 QAFD-RAG 相当（32.80 对 33.50），并优于我们对比中最强的纯结构基准 HippoRAG（EM 提升 5.3，F1 提升 3.4）；在 2WikiMultiHopQA 上，HippoRAG 和 QAFD-RAG 由于其短语-节点架构仍保持优势。禁用门控的消融实验证实，该门控是 F1 分数提升 3.6 到 7.4 个百分点，同时检索延迟降低 1.5 到 4.9 倍的来源。

## Abstract
Retrieval-augmented generation built on knowledge graphs (Graph RAG) outperforms flat passage retrieval on multi-hop question answering by leveraging graph structure. In most existing systems, however, the question only sets the seed nodes; the subsequent traversal becomes "query-blind", depending solely on the graph structure. The exception is QAFD-RAG, which implements query-aware traversal via a flow-diffusion solver with combined edge re-weighting. This architecture requires loading the full graph into Python memory and an iterative solver with a variable number of iterations complicating integration with the graph database. We propose a spreading-activation method that achieves the same query-aware traversal with a single per-step semantic gate: the step weight is the cosine similarity between the candidate entity's description and the question, and the number of iterations is fixed. The whole retrieval procedure - seed mapping, propagation, top-K selection and context assembly - is expressed as a single Cypher query executed in one round-trip to Neo4j; the graph never leaves the database. On MuSiQue our method matches QAFD-RAG by exact match (32.80 vs 33.50) and outperforms the strongest purely-structural baseline in our comparison, HippoRAG, by 5.3 EM and 3.4 F1; on 2WikiMultiHopQA HippoRAG and QAFD-RAG retain an advantage due to their phrase-node architectures. An ablation with the gate disabled confirms that the gate is the source of a simultaneous F1 gain of 3.6 to 7.4 points and a retrieval-latency reduction by a factor of 1.5 to 4.9.

---

## 论文详细总结（自动生成）

这是一份关于论文《Query-Aware Spreading Activation for Multi-Hop Retrieval over Knowledge Graphs》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：在多跳问答（Multi-Hop QA）任务中，现有的 Graph RAG 系统虽然利用了知识图谱（KG）的结构，但大多存在“查询盲目”遍历的问题——即问题仅用于确定初始种子节点，后续的图遍历过程仅依赖图的拓扑结构。
*   **核心挑战**：现有的查询感知方法（如 QAFD-RAG）虽然引入了语义引导，但其算法复杂度高，需要将整个图加载到 Python 内存中运行复杂的迭代求解器，难以与工业级图数据库（如 Neo4j）高效集成。
*   **研究目标**：提出一种既能感知查询语义，又能直接在图数据库内部以单次查询（Single Round-trip）高效执行的检索方法。

### 2. 方法论
*   **核心思想**：将经典的“激活扩散”（Spreading Activation）机制与“语义门控”（Semantic Gate）相结合。在每一轮传播中，不仅考虑结构连接，还通过计算候选节点描述与问题的余弦相似度来动态调整传播权重。
*   **关键技术细节**：
    *   **索引阶段**：使用 LLM（Gemini 2.5 Flash）提取实体和关系，进行实体消解（Entity Resolution），并生成实体描述的向量嵌入。
    *   **种子映射**：将问题中的实体映射到图节点，计算初始激活值。
    *   **查询感知传播**：执行固定次数（通常 $T=3$）的迭代。节点 $v$ 在 $t$ 步获得的激活增量由公式定义：$\Delta r^{(t)}(v) = \alpha \cdot \sigma(v) \cdot \sum r^{(t-1)}(u)$。其中 $\sigma(v)$ 是语义门控（节点描述与问题的余弦相似度），$\alpha$ 是衰减因子。
    *   **单次 Cypher 执行**：整个检索逻辑（种子匹配、多轮传播、路径过滤、Top-K 选择）被封装在一个复杂的 Cypher 查询中，由 Neo4j 查询优化器直接执行，数据无需离开数据库。

### 3. 实验设计
*   **数据集**：
    *   **MuSiQue**：由 2-4 跳单跳问题组合而成，是目前最难的多跳问答基准之一。
    *   **2WikiMultiHopQA**：基于维基百科结构化信息（如表格、信息框）构建，包含“桥接”和“比较”类问题。
*   **Benchmark 与对比方法**：
    *   **纯结构基准**：HippoRAG, PathRAG, GraphRAG, LightRAG。
    *   **查询感知基准**：QAFD-RAG（目前最强的查询感知流扩散方法）。
    *   **消融实验**：包括禁用语义门控（uniform propagation）、改变传播深度 $T$、调整衰减因子 $\alpha$ 等 11 种配置。

### 4. 资源与算力
*   **硬件环境**：MacBook Pro (M1 Pro 芯片, 10核 CPU, 16GB RAM, NVMe SSD)。
*   **软件环境**：Neo4j 5.26 社区版（分配 4GB 堆内存和 2GB 页面缓存）。
*   **模型使用**：
    *   **LLM**：Google Gemini 2.5 Flash（用于实体提取、问题解析和最终答案生成）。
    *   **Embedding**：Google text-embedding-004（768维）。
*   **训练时长**：该方法为非参数化方法，无需训练，主要开销在于索引构建和推理时的 API 调用。

### 5. 实验数量与充分性
*   **实验规模**：在两个数据集上各抽取 1000 个标准样本进行主实验，消融实验则在每数据集前 500 个样本上运行。
*   **充分性评价**：
    *   **多维度覆盖**：实验涵盖了准确率（F1, EM）、检索延迟、Token 消耗等关键指标。
    *   **消融充分**：通过对比“无门控”版本，清晰地证明了语义门控在提升质量的同时降低了计算量。
    *   **客观性**：作者指出了在 2Wiki 数据集上表现不如 HippoRAG 的原因，显示了客观的科研态度。

### 6. 主要结论与发现
*   **双重增益**：引入查询感知的语义门控能**同时**提高准确率并降低延迟。在 MuSiQue 上 F1 提升 7.4，延迟降低 1.5-4.9 倍。
*   **性能匹配**：在 MuSiQue 这种复杂的链式推理任务中，该方法在 EM 指标上基本持平了复杂的 QAFD-RAG，且显著优于 HippoRAG。
*   **深度饱和**：实验发现传播深度 $T=3$ 是性能与成本的最佳平衡点，进一步增加深度会导致噪声引入和延迟激增。

### 7. 优点
*   **工程友好性**：完全基于 Cypher 语言实现，无需在 Python 层维护复杂的图状态，易于在生产环境中部署和扩展。
*   **高效性**：通过语义门控在传播过程中实时剪枝（Pruning），避免了激活扩散在全图范围内的盲目扩张。
*   **内存节省**：不需要将全图加载到 RAM 中，图规模仅受数据库存储限制。

### 8. 不足与局限
*   **实体类型限制**：索引阶段仅提取了 5 类预定义实体，导致 2Wiki 中一些非实体类事实（如日期、职业、国籍）无法作为节点被激活，限制了在特定类型问题上的表现。
*   **比较类问题弱势**：对于需要并行查找两个实体属性并进行逻辑比较的问题（如“谁出生更早”），单纯的路径遍历效果有限。
*   **模型依赖**：语义门控的效果高度依赖于 LLM 生成的实体描述质量以及 Embedding 模型的判别能力，文中未测试不同模型组合的影响。

（完）
