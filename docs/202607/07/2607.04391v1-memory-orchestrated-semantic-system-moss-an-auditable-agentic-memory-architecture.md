---
title: "Memory-Orchestrated Semantic System (MOSS): An Auditable Agentic Memory Architecture"
title_zh: 记忆编排语义系统 (MOSS)：一种可审计的智能体记忆架构
authors: "Serge Lacasse, Jérémie Hatier, Alex Baker"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04391v1"
tags: ["query:rag"]
score: 7.5
evidence: 作为标准RAG替代方案的智能体记忆架构
tldr: 针对AI智能体长期记忆中RAG技术不透明、难审计的问题，本文提出MOSS架构。MOSS通过智能体驱动结构化关系数据库检索，实现符号化、可复现且模型无关的记忆管理。经一年真实生产环境测试，证明了其在处理大规模语料时的可审计性和主权性，为长期伴随式AI提供了新的记忆范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04391v1/fig-001.webp\", \"caption\": \"Figure 1: MOSS deployment architecture. The Orchestrator holds all active code—REST API / MCP, connectors to LLM providers, the dynamic QueryProfiler, and deterministic SQL retrieval— and is the single point of contact between agents and memory. The Memory Structure is the relational database itself. The system is model-agnostic (left: hosted and local providers), engineagnostic (any relational database, centre), and storage-agnostic (right: cloud, enterprise object storage, Microsoft 365, on-premise). Deployment is local or cloud, with no vendor lock-in, and compatible with any existing infrastructure.\", \"page\": 7, \"index\": 1, \"width\": 956, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2607.04391v1/fig-002.webp\", \"caption\": \"Figure 2: The query lifecycle and continuous indexing. Reading (centre): the agent analyzes the query’s intent and parameterizes the QueryProfiler, which resolves the request through deterministic, auditable SQL retrieval over the Memory Structure, with no LLM in the loop; the agent then evaluates the results, reformulating if needed (dashed iteration loop) before formulating the answer. Non-determinism is confined to the agentic edges, while the deterministic core is traversed but never iterated. Writing (right): content leaving the active (sliding) window is indexed into the database in real time and archived to the conversation file, while imported documents and conversation histories are indexed retroactively—so the memory grows continuously and remains structurally unbounded.\", \"page\": 10, \"index\": 2, \"width\": 897, \"height\": 731}]"
motivation: 现有的RAG技术依赖向量相似度搜索，存在不透明、难以审计且受向量表示限制等结构性缺陷。
method: 提出一种由智能体驱动、基于结构化关系数据库的记忆架构，通过符号化查询实现可复现的检索，并从语料中归纳概念词汇。
result: 在长达一年的个人学者语料库（含4400万Token及数百万关系）的生产部署中，验证了系统的稳定性与高效性。
conclusion: 可审计、主权且结构无界的记忆系统是AI智能体实现跨年度长期伴随而非短期会话的关键前提。
---

## 摘要
长期记忆仍然是 AI 智能体的结构性弱点。主流的检索增强生成 (RAG) 方法依赖于基于嵌入的相似度搜索，这种方式在构建上是不透明的，难以审计，且受限于向量表示的理论极限。我们提出了记忆编排语义系统 (MOSS)，这是一种智能体记忆架构，由智能体驱动对结构化关系数据库的检索。MOSS 具有模型无关、存储无关和 API 无关的特性：它可以在任何关系引擎上运行，连接到任何大语言模型 (LLM) 提供商（或确定性的非 LLM 进程），并部署在任何本地或云端基础设施上。其检索执行是符号化且可复现的（一旦查询确定，LLM 就不再参与检索循环），并且系统的每一步（从索引到答案生成）都会被记录并可供检查，使 MOSS 在构建上具备可审计性。MOSS 并非强加外部本体，而是从语料库本身推导出其概念词汇。我们报告了一项在智能体记忆文献中独一无二的长期部署：在一位学者的工作语料库上进行了一年的持续生产运行——该对话语料库可追溯至 2024 年 10 月（约 4400 万个 token，进行了追溯索引），包含 110,183 个片段，以及 163,494 份编目文档、569 个归纳得出的概念、322,662 个概念注释和 11 个元数据图（总计约 500 万个关系），跨越了四个连续的基础设施代际。虽然本案例针对的是单个研究人员，但该架构绝非仅限于个人：它可服务于团队、机构或任何随时间积累知识的实体。我们认为，可审计、主权且结构上无限制的记忆，是旨在陪伴个人或组织数年而非仅数个会话的 AI 智能体的先决条件。

## Abstract
Long-term memory remains a structural weakness of AI agents. The dominant approach, retrieval-augmented generation (RAG), relies on embedding-based similarity search, which is opaque by construction, difficult to audit, and bounded by the theoretical limits of vector representations. We present the Memory-Orchestrated Semantic System (MOSS), an agentic memory architecture in which the agent drives retrieval over a structured relational database. MOSS is model-agnostic, storage-agnostic, and API-agnostic: it runs on any relational engine, connects to any LLM provider (or to deterministic non-LLM processes), and deploys on any infrastructure, local or cloud. Its retrieval execution is symbolic and reproducible (once a query is formulated, no LLM participates in the retrieval loop) and every step of the system, from indexing to answer formulation, is logged and inspectable, making MOSS auditable by construction. Rather than imposing an external ontology, MOSS derives its conceptual vocabulary from the corpus itself. We report on a longitudinal deployment unique in the agentic-memory literature: a year of continuous production over an individual scholar's working corpus--a conversational corpus reaching back to October 2024 (some 44 million tokens, retroactively indexed) comprising 110,183 segments, alongside 163,494 catalogued documents, 569 inductively derived concepts, 322,662 concept annotations, and eleven metadata graphs totaling approximately five million relations--across four successive infrastructure generations. While the present case is that of a single researcher, the architecture is in no way specific to one person: it serves a team, an institution, or any entity that accumulates knowledge over time. We argue that auditable, sovereign, structurally unbounded memory is a precondition for AI agents intended to accompany a person or an organization over years rather than sessions.