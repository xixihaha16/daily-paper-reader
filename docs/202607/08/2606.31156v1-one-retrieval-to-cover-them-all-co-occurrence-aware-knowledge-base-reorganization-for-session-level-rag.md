---
title: "One Retrieval to Cover Them All: Co-occurrence-Aware Knowledge Base Reorganization for Session-Level RAG"
title_zh: 一次检索覆盖全局：面向会话级 RAG 的感知共现知识库重组
authors: "Shivam Ratnakar, Yixuan Zhu, Cecilia Cheng, Chaya Vijayakumar"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31156v1"
tags: ["query:rag"]
score: 8.0
evidence: 通过知识库重组实现会话级RAG
tldr: "针对企业级RAG系统中单次检索难以覆盖用户会话级需求的问题，本文提出了一种基于共现感知的知识库重组方法。通过离线聚类和在线邻域扩展，该方法在WixQA数据集上将单次检索的会话覆盖率从41%提升至58%，并显著减少了达到目标覆盖率所需的检索次数，同时实现了知识库的高效压缩。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31156v1/fig-001.webp\", \"caption\": \"Figure 1: The session coverage gap. A user setting up a website needs articles about domains, templates, and payments, but these reside in distant regions of the semantic embedding space. Vanilla RAG retrieves only documents within the top-k similarity radius of the query (dashed circle), missing semantically distant but session-relevant articles. Our cluster expansion recovers these, raising session coverage from 41% to 58%.\", \"page\": 1, \"index\": 1, \"width\": 446, \"height\": 330}, {\"url\": \"assets/figures/arxiv/2606.31156v1/fig-002.webp\", \"caption\": \"Figure 4: Retrieval calls to reach session coverage thresholds. Hybrid retrieval requires 26 to 36% fewer calls across all targets.\", \"page\": 6, \"index\": 2, \"width\": 419, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2606.31156v1/fig-003.webp\", \"caption\": \"Figure 5: Semantic similarity vs. co-occurrence strength (schematic from 10K sampled pairs). The red triangle region (low similarity, high co-occurrence) contains document pairs that vanilla RAG cannot surface together. Weak Pearson correlation (r=0.20) confirms the signals are complementary.\", \"page\": 7, \"index\": 3, \"width\": 419, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2606.31156v1/fig-004.webp\", \"caption\": \"Table 12: Full hyperparameter configuration.\", \"page\": 10, \"index\": 4, \"width\": 433, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2606.31156v1/fig-005.webp\", \"caption\": \"Figure 2: System overview. (a) Offline: Documents from the same user session (same color) are scattered in semantic embedding space but cluster together after Word2Vec training on co-occurrence sequences. (b) Online: Given a query, we retrieve the top-3 by similarity (gray dots), expand through their cluster neighborhoods (blue dots, +12 candidates), and re-rank the union to return the top-8 with 58% session coverage.\", \"page\": 4, \"index\": 5, \"width\": 880, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2606.31156v1/fig-006.webp\", \"caption\": \"Figure 3: Session coverage vs. retrieval budget. The shaded area represents the coverage gap that cluster expansion closes. Notably, the gap widens with k.\", \"page\": 5, \"index\": 6, \"width\": 431, \"height\": 299}]"
motivation: 传统的RAG系统仅针对单次查询进行优化，无法有效满足企业用户在连续会话中跨语义领域的复杂信息需求。
method: 采用共现感知聚类技术离线重组知识库，并在查询时通过聚类邻域扩展检索候选集。
result: "在WixQA数据集上，该方法将单次检索的会话覆盖率提升了17%，减少了34%的检索调用次数，并将知识库压缩至原大小的20%。"
conclusion: 会话级覆盖率应取代单次查询召回率，成为评估企业级RAG系统的核心指标。
---

## 摘要
RAG 系统检索文档时，通常针对一次回答一个查询进行优化。然而，企业用户通常以“会话”形式进行交互，即由一系列相关问题组成的连贯片段，这些问题可能跨越知识库中语义距离较远的部分。我们研究表明，在标准知识库上进行单次检索调用仅能覆盖用户会话级信息需求的 41%。为了弥补这一差距，我们利用感知共现的聚类方法对知识库进行离线重组，并在查询时通过聚类邻域扩展检索候选对象。在 WixQA（包含 6,221 篇企业支持文章）上，我们的方法将单次查询的会话覆盖率提高到 58%（绝对提升 +17%；95% 置信区间：[14.1, 20.4]），将达到 70% 覆盖率所需的检索调用次数减少了 34%，并将知识库压缩至原始大小的 20%，且在四种嵌入模型和六个功能领域中均表现一致。我们认为，会话级覆盖率而非单次查询召回率，应成为企业级 RAG 评估的主要指标。

## Abstract
RAG systems retrieve documents optimized for answering one query at a time. Yet enterprise users arrive with sessions, that is, coherent episodes of related questions that span semantically distant parts of the knowledge base. We show that a single retrieval call over a standard knowledge base covers only 41% of a user's session-level information need. To close this gap, we reorganize the KB offline using co-occurrence-aware clustering and expand retrieval candidates through cluster neighborhoods at query time. On WixQA (6,221 enterprise support articles), our method raises single-query session coverage to 58% (+17% absolute; 95% CI: [14.1, 20.4]), reduces retrieval calls to 70% coverage by 34%, and compresses the KB to 20% of its original size, all consistently across four embedding models and six functional domains. We argue that session-level coverage, not single-query recall, should be the primary metric for enterprise RAG evaluation.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对企业级检索增强生成（RAG）系统的创新优化方案，旨在解决传统 RAG 在处理用户“会话级”需求时的局限性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 RAG 系统主要针对**单次查询的语义相关性**进行优化，即检索与当前问题最相似的文档。然而，企业用户（如客服咨询）通常处于一个**会话（Session）**中，其需求往往跨越知识库中语义距离较远的多个领域（例如：用户问“如何连接域名”，其实后续还需要“设置支付”和“选择模板”）。
*   **研究动机**：语义相似度并不等同于用户的信息需求。在语义空间中，功能相关的文档可能相距甚远。实验表明，传统 RAG 的单次检索仅能覆盖用户会话需求的 41%，导致用户必须多次提问、多次触发检索，增加了延迟和成本。

### 2. 方法论：共现感知知识库重组
该方法分为离线重组和在线检索两个阶段：
*   **离线阶段：共现感知聚类（Co-occurrence-Aware Clustering）**
    1.  **序列构建**：通过三种来源生成文档 ID 序列：专家标注的共现真值、基于嵌入空间的随机游走（高跳转率以捕获跨领域关系）、以及基于 QA 对的模拟浏览路径。
    2.  **共现嵌入**：借鉴 Item2Vec 的思路，将文档 ID 视为 Token，使用 Word2Vec (CBOW) 训练文档的“共现向量”。这使得在同一会话中经常被需要的文档在向量空间中相互靠近。
    3.  **层次聚类**：对共现向量进行凝聚层次聚类，将知识库重组为多个聚类（Cluster），目标是将知识库有效规模压缩至 20%。
*   **在线阶段：混合检索与聚类扩展**
    1.  **初始检索**：使用任何标准的嵌入模型检索 Top-$k_v$ 个文档。
    2.  **邻域扩展**：获取这 $k_v$ 个文档所属的聚类中的所有其他文档作为候选。
    3.  **重排序**：将初始结果与扩展候选合并，按原始查询相似度重新排序，返回最终的 Top-$k$ 结果。

### 3. 实验设计
*   **数据集**：
    *   **WixQA**：核心基准，包含 6,221 篇企业支持文章和 400 个专家标注的复杂查询。
    *   **E-Commerce Support**：包含 1,000 个文档的电商客服语料，用于跨领域验证。
    *   **HotpotQA**：用于边界条件测试（测试无文档复用场景下的表现）。
*   **Benchmark 与对比方法**：
    *   对比了 **Vanilla RAG**（标准向量检索）和 **Cross-encoder Reranking**（交叉编码器重排序）。
    *   使用了 4 种主流嵌入模型（MiniLM, BGE, GTE, E5）验证通用性。
*   **评估指标**：提出了**单次查询会话覆盖率 (Cov)** 和 **达到 $\tau$ 覆盖率所需的检索调用次数 ($C_\tau$)**。

### 4. 资源与算力
*   **算力消耗**：该方法对算力要求极低。
    *   **离线流程**：Word2Vec 训练在单核 CPU 上不到 30 秒；聚类约 2 分钟；整个离线管线（含 QA 生成）在 20 分钟内完成。
    *   **在线开销**：聚类查找为 $O(1)$，重排序仅增加约 15 个候选文档，延迟增加可忽略不计。
*   **成本**：使用 GPT-4.1-nano 生成合成数据的成本约为 2 美元。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种嵌入模型、6 个功能领域（如支付、域名、编辑器等）、4 种会话复杂度（简单到极复杂）。
*   **充分性与客观性**：
    *   实验通过 1,000 次自助法（Bootstrap）重采样计算置信区间，确保结果的统计显著性。
    *   通过 HotpotQA 的负面实验证明了该方法并非盲目增加召回，而是依赖于知识库的内在共现结构。
    *   跨数据集验证（电商语料）增强了结论的说服力。

### 6. 主要结论与发现
*   **覆盖率提升**：在 $k=8$ 时，单次检索的会话覆盖率从 41% 提升至 **58%**（绝对提升 17%）。
*   **效率提升**：达到 70% 覆盖率所需的检索调用次数减少了 **34%**。
*   **知识库压缩**：在提升性能的同时，将知识库的有效结构压缩了 80%（20% 原始大小）。
*   **模型无关性**：在所有测试的嵌入模型上均表现出一致的增益（+17% 到 +21%）。

### 7. 优点
*   **预检索优化**：不同于大多数在检索后做文章的方法，它通过优化知识库组织结构，从根本上解决了“找不全”的问题。
*   **即插即用**：不依赖特定的嵌入模型，可以与现有的任何 RAG 流程结合。
*   **低成本高收益**：离线计算开销极小，但能显著减少在线 API 调用次数和用户交互摩擦。
*   **指标创新**：提出了更符合企业实际场景的“会话级”评估指标。

### 8. 不足与局限
*   **合成数据偏差**：由于缺乏公开的企业用户真实导航日志，共现序列主要依赖合成数据和随机游走，可能与真实用户行为存在偏差。
*   **生成质量未评估**：论文重点在于检索覆盖率，尚未通过 LLM-as-judge 评估更多上下文是否会导致 LLM 出现“迷失在中间”（Lost in the Middle）的现象。
*   **应用限制**：该方法依赖于文档的重复使用和共现模式，对于一次性、无关联的文档集合（如 HotpotQA）无效。

（完）
