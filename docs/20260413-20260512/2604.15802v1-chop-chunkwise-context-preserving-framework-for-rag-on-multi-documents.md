---
title: "CHOP: Chunkwise Context-Preserving Framework for RAG on Multi Documents"
title_zh: CHOP：面向多文档检索增强生成（RAG）的块级上下文保留框架
authors: "Hyunseok Park, Jihyeon Kim, Jongeun Kim, Dongsik Yoon"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15802v1"
tags: ["query:rag"]
score: 9.0
evidence: RAG的块级上下文保留框架
tldr: "针对检索增强生成（RAG）中相似文档导致的检索精度下降和幻觉问题，本文提出CHOP框架。该框架通过CNM-Extractor提取块级元数据并利用连续性决策模块保持上下文连贯性。通过在分块前添加上下文感知元数据，CHOP显著降低了语义冲突，在基准测试中实现了90.77%的Top-1命中率，有效提升了多文档环境下的检索质量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15802v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the CHOP architecture comprising two components: the Continuity Decision module, which determines whether consecutive chunks are continuous, and theCNM-Extractor, which generates compact representations.When continuity holds (left), the next chunk inherits the previous CNM; when it does not (right), a new CNM is extracted. Each chunk is then prefixed with its CNM, and the combined representations are embedded to construct the vector database.\", \"page\": 2, \"index\": 1, \"width\": 1050, \"height\": 394}]"
motivation: 解决RAG系统中因多个相似文档共存导致的检索混淆、信息冗余及事实错误问题。
method: 提出包含CNM-Extractor提取块签名和连续性决策模块保持上下文的框架，通过为分块添加元数据前缀来增强检索辨识度。
result: "在基准数据集上达到了90.77%的Top-1命中率，并显著提升了排序质量指标。"
conclusion: CHOP框架通过保留分块上下文信息，为构建高质量、可扩展的知识库提供了一种有效的方法。
---

## 摘要
检索增强生成（RAG）系统在向量数据库中存在相似文档时会损失检索准确性，从而导致冗余信息、幻觉和事实错误。为了缓解这一问题，我们提出了 CHOP，这是一个利用大语言模型（LLM）迭代评估数据块相关性，并通过确定其与特定主题或查询类型的关联来逐步重构文档的框架。CHOP 集成了两个关键组件：CNM-Extractor，用于生成捕获类别、关键名词和模型名称的紧凑型单块签名；以及连续性决策模块（Continuity Decision Module），通过判断连续数据块是否属于同一文档流来保持上下文连贯性。通过为每个数据块添加上下文感知元数据前缀，CHOP 减少了相似文档之间的语义冲突，并增强了检索器的辨别能力。在基准数据集上的实验表明，CHOP 缓解了检索混淆，并为构建高质量知识库提供了一种可扩展的方法，实现了 90.77% 的 Top-1 命中率，并在排序质量指标上取得了显著提升。

## Abstract
Retrieval-Augmented Generation (RAG) systems lose retrieval accuracy when similar documents coexist in the vector database, causing unnecessary information, hallucinations, and factual errors. To alleviate this issue, we propose CHOP, a framework that iteratively evaluates chunk relevance with Large Language Models (LLMs) and progressively reconstructs documents by determining their association with specific topics or query types. CHOP integrates two key components: the CNM-Extractor, which generates compact per-chunk signatures capturing categories, key nouns, and model names, and the Continuity Decision Module, which preserves contextual coherence by deciding whether consecutive chunks belong to the same document flow. By prefixing each chunk with context-aware metadata, CHOP reduces semantic conflicts among similar documents and enhances retriever discrimination. Experiments on benchmark datasets show that CHOP alleviates retrieval confusion and provides a scalable approach for building high-quality knowledge bases, achieving a Top-1 Hit Rate of 90.77% and notable gains in ranking quality metrics.

---

## 论文详细总结（自动生成）

以下是对论文《CHOP: Chunkwise Context-Preserving Framework for RAG on Multi Documents》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的检索增强生成（RAG）系统在处理包含大量相似文档（如不同型号但功能相近的产品手册）的向量数据库时，检索精度会显著下降。
*   **研究背景**：
    *   **语义冲突**：相似文档共存会导致检索器难以区分特定型号的信息，引发幻觉和事实错误。
    *   **上下文断裂**：基于长度的固定分块（Chunking）会切断文档内部的指代关系（如“该方法”、“图3”），导致分块失去解释性。
    *   **检索歧义**：多文档环境下的冗余和重复内容增加了检索的模糊性。

### 2. 论文提出的方法论：CHOP 框架
CHOP（Chunkwise Context-Preserving）框架的核心思想是通过 LLM 为每个数据块生成并维护一个“上下文感知元数据前缀”，以增强分块的辨识度和连贯性。

*   **CNM-Extractor（元数据提取器）**：
    *   为每个分块提取三元组特征 **CNM**：**Category**（类别，如空调）、**Nouns**（关键名词，如过滤器）、**Model**（型号，如 X-SERIES）。
    *   这些元数据作为前缀注入分块，稳定语义框架并明确实体指代。
*   **Continuity Decision Module（连续性决策模块）**：
    *   **核心逻辑**：利用 LLM 判断相邻分块 $C_i$ 和 $C_{i+1}$ 是否属于同一文档流或同一主题。
    *   **继承机制**：如果判定为“连续”（TRUE），则 $C_{i+1}$ 直接继承 $C_i$ 的 CNM 元数据；如果判定为“不连续”（FALSE），则为 $C_{i+1}$ 重新提取新的 CNM。
    *   **作用**：防止主题漂移，确保长文档内部的语义一致性，并明确区分不同文档的边界。
*   **工作流程**：输入文档分块 -> 连续性判定 -> 提取/继承 CNM -> 拼接前缀 -> 向量化存储。

### 3. 实验设计
*   **数据集**：使用 **MRAMG-Bench**（源自 ManualsLib 的产品手册基准）。研究者将其重构为连续的长文件，以模拟真实世界中缺乏明确文档边界的复杂场景。
*   **对比方法（Baselines）**：
    *   **Naive-500T**：固定长度分块（500 token，100 token 重叠）。
    *   **Cosine-Chunking**：基于句子间余弦相似度阈值的自适应分块。
*   **评估指标**：
    *   **检索性能**：Hit Rate@K、MRR@K、NDCG@K。
    *   **生成性能**：F1、ROUGE-L、BERTScore。

### 4. 资源与算力
*   **模型使用**：
    *   **Backbone LLM**：使用 **Gemma-12B** 进行分类和元数据提取。
    *   **Embedding**：使用 OpenAI 的 `text-embedding-3-large`（3072维）。
    *   **数据库**：ChromaDB (HNSW 索引)。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及处理总时长。实验中将 LLM 温度设为 0 以保证结果的可复现性。

### 5. 实验数量与充分性
*   **实验规模**：针对检索和生成两个阶段分别进行了多组 Top-K（1, 3, 5, 10）的对比实验。
*   **充分性评价**：
    *   实验涵盖了从基础检索指标到语义生成指标的全方位评估。
    *   通过重构数据集模拟了高难度、高重叠的检索场景，具有较强的实战参考价值。
    *   **不足之处**：缺乏针对不同规模 LLM（如 7B vs 70B）作为提取器的消融实验，也未深入探讨不同 Embedding 模型对前缀敏感度的影响。

### 6. 主要结论与发现
*   **检索精度大幅提升**：CHOP 在 Top-1 Hit Rate 上达到了 **90.77%**，远高于 Naive-500T (81.28%) 和 Cosine-Chunking (70.77%)。
*   **排序质量更优**：MRR 和 NDCG 指标的提升表明，CHOP 不仅能找回正确证据，还能将其排在更靠前的位置。
*   **生成质量改善**：更准确的检索直接转化为更高质量的回答，F1 和 ROUGE-L 指标在 Top-3/5/10 设定下均有稳定增益。
*   **预算效率**：在相同的上下文预算下，CHOP 能够提供更有效的证据，减少了无关信息对生成阶段的干扰。

### 7. 优点
*   **无需重新训练**：通过在数据预处理阶段添加元数据前缀，无需改动现有的检索算法或模型权重即可提升性能。
*   **上下文保留**：巧妙地解决了长文档分块后的指代不明问题，通过“继承机制”维持了语义的连贯性。
*   **抗干扰能力强**：在处理高度相似的多个产品手册时，CNM 前缀起到了“语义锚点”的作用，有效减少了向量空间的碰撞。

### 8. 不足与局限
*   **预处理成本高**：每个分块都需要经过 LLM 进行连续性判断和元数据提取，对于海量文档库，其计算开销和时间成本较高。
*   **依赖 LLM 质量**：如果 LLM 在提取 CNM 或判断连续性时出错，可能会导致错误的元数据被传播到后续所有分块，产生连锁反应。
*   **应用限制**：该方法主要针对结构化较强、具有明确型号/类别的技术文档（如手册、法规），在文学创作或非结构化杂乱文本上的普适性有待验证。

（完）
