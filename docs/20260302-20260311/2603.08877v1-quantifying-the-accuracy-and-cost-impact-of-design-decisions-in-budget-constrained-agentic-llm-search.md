---
title: Quantifying the Accuracy and Cost Impact of Design Decisions in Budget-Constrained Agentic LLM Search
title_zh: 量化预算受限的智能体大语言模型搜索中设计决策对准确率和成本的影响
authors: "Kyle McCleary, James Ghawaly"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08877v1"
tags: ["query:rag"]
score: 9.0
evidence: 具有迭代搜索和规划的智能体RAG系统
tldr: 本研究探讨了在预算受限（如工具调用次数和Token数限制）的情况下，Agentic RAG系统的设计决策对准确率和成本的影响。通过引入模型无关的评估框架BCAS，研究者在多个LLM和基准测试上量化了搜索深度、检索策略及生成预算的作用。结果表明，混合检索结合轻量级重排序效果最佳，且增加搜索次数在一定范围内能显著提升性能，为构建高效且经济的智能体检索流水线提供了实践指导。
selection_source: fresh_fetch
motivation: 现有的Agentic RAG系统在实际部署中面临严格的工具调用和Token预算限制，但缺乏关于设计决策如何影响成本与准确率权衡的量化研究。
method: 提出了BCAS评估框架，通过在六种大模型和三个问答基准上控制搜索深度、检索策略和生成预算等变量进行对比实验。
result: 实验发现准确率随搜索次数增加而提升但存在上限，混合检索配合重排序增益最大，且较大的生成预算对复杂合成类任务尤为重要。
conclusion: 该研究为预算受限下的智能体检索系统配置提供了实用的量化指导，并发布了可复现的提示词和评估设置。
---

## 摘要
智能体检索增强生成（RAG）系统结合了迭代搜索、规划提示词和检索后端，但在实际部署场景中，工具调用和生成 Token 数量受到明确的预算限制。我们提出了一项受控测量研究，探讨在固定约束下，搜索深度、检索策略和生成预算如何影响准确率和成本。利用预算受限智能体搜索（BCAS）——一种能够显示剩余预算并管控工具使用的模型无关评估框架，我们在六种大语言模型（LLM）和三个问答基准测试上进行了对比实验。在不同模型和数据集上，准确率随搜索次数增加而提升，直至达到一个较小的上限；在消融实验中，结合轻量级重排序的混合词法与稠密检索带来了最大的平均增益；而更大的生成预算对 HotpotQA 风格的综合任务最为有效。这些结果为配置受限预算下的智能体检索流水线提供了实践指导，并附带了可复现的提示词和评估设置。

## Abstract
Agentic Retrieval-Augmented Generation (RAG) systems combine iterative search, planning prompts, and retrieval backends, but deployed settings impose explicit budgets on tool calls and completion tokens. We present a controlled measurement study of how search depth, retrieval strategy, and completion budget affect accuracy and cost under fixed constraints. Using Budget-Constrained Agentic Search (BCAS), a model-agnostic evaluation harness that surfaces remaining budget and gates tool use, we run comparisons across six LLMs and three question-answering benchmarks. Across models and datasets, accuracy improves with additional searches up to a small cap, hybrid lexical and dense retrieval with lightweight re-ranking produces the largest average gains in our ablation grid, and larger completion budgets are most helpful on HotpotQA-style synthesis. These results provide practical guidance for configuring budgeted agentic retrieval pipelines and are accompanied by reproducible prompts and evaluation settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Quantifying the Accuracy and Cost Impact of Design Decisions in Budget-Constrained Agentic LLM Search》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：检索增强生成（RAG）已从静态流水线演变为能够自主规划和执行多步检索的“智能体（Agentic）”系统。
*   **核心问题**：尽管智能体检索提升了准确率，但在实际部署中，API 调用成本、Token 消耗和延迟是关键约束。现有研究多关注无限制下的准确率，缺乏在**固定预算（工具调用次数和 Token 限制）**下对设计决策（如搜索深度、检索策略、规划/反思机制）如何影响性能与成本权衡的量化分析。
*   **研究动机**：为开发者提供一套实用的指南，指导如何在有限预算内配置最优的智能体检索流水线。

### 2. 方法论：BCAS 框架
论文提出了 **BCAS (Budget-Constrained Agentic Search)**，一个模型无关的评估框架：
*   **核心思想**：将预算约束（剩余搜索次数、剩余 Token 数）作为显式信号输入给模型，并强制执行这些约束。
*   **执行循环**：
    1.  **推理（Reason）**：模型生成“Thought”，分析当前进度及所需信息。
    2.  **工具选择（Select Tool）**：从动态列表中选择动作（如搜索数据库），若搜索预算耗尽，则强制移除搜索工具。
    3.  **执行与观察（Execute & Observe）**：执行检索并返回结果。
    4.  **预算更新（Update Budget）**：实时记录 Token 和搜索次数，更新剩余额度。
*   **关键组件**：
    *   **可选规划与反思**：预先分解问题（Pre-planning）或周期性审查进度（Reflection）。
    *   **检索策略**：支持 BM25（词法）、混合检索（HS，词法+向量）以及重排序（RR）。

### 3. 实验设计
*   **实验对象**：6 种主流 LLM（o4-mini, DeepSeek V3, GPT-4.1-mini, Gemma 3 27B, Qwen 3 14B, LLaMA 3.1 8B）。
*   **基准测试集**：
    *   **TriviaQA**：单跳事实检索。
    *   **HotpotQA**：多跳推理与信息合成。
    *   **2WikiMultihopQA**：复杂实体关系的多跳推理。
*   **对比维度**：
    *   **搜索深度**：1, 2, 3 次及无限制搜索。
    *   **Token 预算**：500, 1K, 2K, 4K, 16K。
    *   **组件消融**：规划、反思、混合检索、重排序的组合效果。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 训练时长或型号，因为该研究侧重于**推理阶段的测量**而非模型训练。
*   **技术栈**：检索后端基于 **ParadeDB**（PostgreSQL 扩展），向量嵌入使用 **BGE-M3** 模型，重排序使用 **bge-reranker-v2-m3**。
*   **评估成本**：通过 GPT-4o-mini 作为自动评测法官（LLM-as-a-judge），并进行了人工审计以确保准确性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个模型在 3 个数据集上的多种配置组合，每组实验样本量约为 500 个，总计运行了数千次推理轨迹。
*   **消融实验**：在 HotpotQA 上进行了全因子的组件消融实验（规划、反思、HS、RR）。
*   **客观性与公平性**：
    *   所有模型使用**统一的提示词模板**，不针对特定模型调优，以确保比较的是框架本身的敏感性。
    *   对 600 个样本进行了人工审计，验证了自动评分与人工评分的高度一致性（TriviaQA 100% 一致）。

### 6. 主要结论与发现
*   **搜索深度的边际效应**：准确率随搜索次数增加而提升，但在 **3 次搜索**后通常进入平台期。
*   **检索组件的优先级**：**混合检索 + 轻量级重排序**是提升准确率最有效的手段（平均提升 9.29%）。
*   **模型能力的“平替”**：小模型（如 LLaMA 3.1 8B）通过增加搜索次数，可以达到甚至超过大模型在单次搜索下的表现。
*   **Token 预算的影响**：较大的 Token 预算主要对需要复杂合成的任务（如 HotpotQA）有帮助；在极度受限（500 Token）时，模型会变得更简洁并倾向于通过多次搜索来弥补。
*   **o4-mini 的特殊性**：作为推理模型，其内置的思维链使得外部规划组件的增益不明显，但在复杂任务中，反思机制仍能带来显著提升。

### 7. 优点与亮点
*   **实用性强**：直接回答了开发者最关心的“钱该花在哪”的问题（建议：先加搜索次数，再加重排序，最后加 Token 长度）。
*   **预算感知设计**：将预算作为 Prompt 的一部分，诱导模型产生更具策略性的行为（如在预算不足时提前终止）。
*   **模型无关性**：BCAS 框架可以轻松适配任何支持工具调用的 LLM。

### 8. 不足与局限
*   **数据集局限**：主要基于静态事实问答基准，未涉及知识更新极快的开放域网页搜索或多模态场景。
*   **提示词偏差**：使用统一提示词可能导致某些模型（如针对特定格式优化的模型）无法发挥最佳性能。
*   **评分局限**：采用二元（正确/错误）评分，可能忽略了推理过程中的部分正确性或逻辑质量。
*   **非智能体基线缺失**：未直接对比纯静态 RAG（单次检索）与智能体 RAG 在同等 Token 消耗下的效率差异。

（完）
