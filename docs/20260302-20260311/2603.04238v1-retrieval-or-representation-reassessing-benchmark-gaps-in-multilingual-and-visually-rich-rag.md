---
title: Retrieval or Representation? Reassessing Benchmark Gaps in Multilingual and Visually Rich RAG
title_zh: 检索还是表示？重新评估多语言及视觉丰富 RAG 中的基准差距
authors: "Martin Asenov, Kenza Benkirane, Dan Goldwater, Aneiss Ghodsi"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04238v1"
tags: ["query:rag"]
score: 9.0
evidence: 重新评估多语言和视觉丰富RAG中的基准差距
tldr: 本研究重新评估了多语言及视觉丰富文档在检索增强生成（RAG）中的性能差异。虽然端到端多模态检索器声称优于传统的BM25，但作者发现文档表示（如转录和预处理）才是性能提升的核心驱动力。通过在固定检索机制下优化预处理，BM25在多个基准测试中缩小了与先进模型的差距。研究呼吁建立分解式评估基准，以准确区分转录与检索能力的贡献。
selection_source: fresh_fetch
motivation: 探究多模态检索器在处理复杂视觉和多语言文档时，其性能优势究竟源于检索算法的改进还是文档表示的优化。
method: 在保持检索机制不变的前提下，通过系统性地改变文档的转录和预处理方法，重新评估传统BM25与现代检索器的性能差距。
result: 实验证明，仅通过改进文档表示，传统的BM25就能在多语言和视觉基准测试中恢复巨大的性能差距。
conclusion: 文档表示是驱动RAG性能提升的主要因素，评估基准应将转录与检索能力分开衡量，以便更准确地归因技术进步。
---

## 摘要
检索增强生成 (RAG) 是将语言模型与外部文档和最新信息相结合的一种常用方法。经典的检索系统依赖于诸如 BM25 之类的词法方法，这些方法通过词项重叠和语料库级权重对文档进行排序。在大型查询-文档数据集上训练的端到端多模态检索器声称，相比这些方法有显著改进，特别是在具有复杂视觉布局的多语言文档方面。我们证明了更好的文档表示是基准性能提升的主要驱动因素。通过在保持检索机制不变的情况下系统地改变转录和预处理方法，我们证明了 BM25 可以在多语言和视觉基准上弥补巨大的差距。我们的研究结果呼吁建立分解的评估基准，分别衡量转录和检索能力，从而使该领域能够正确归因进展，并将精力集中在关键之处。

## Abstract
Retrieval-augmented generation (RAG) is a common way to ground language models in external documents and up-to-date information. Classical retrieval systems relied on lexical methods such as BM25, which rank documents by term overlap with corpus-level weighting. End-to-end multimodal retrievers trained on large query-document datasets claim substantial improvements over these approaches, especially for multilingual documents with complex visual layouts. We demonstrate that better document representation is the primary driver of benchmark improvements. By systematically varying transcription and preprocessing methods while holding the retrieval mechanism fixed, we demonstrate that BM25 can recover large gaps on multilingual and visual benchmarks. Our findings call for decomposed evaluation benchmarks that separately measure transcription and retrieval capabilities, enabling the field to correctly attribute progress and focus effort where it matters.

---

## 论文详细总结（自动生成）

这是一份关于论文《Retrieval or Representation? Reassessing Benchmark Gaps in Multilingual and Visually Rich RAG》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在处理多语言和视觉丰富（如图表、复杂布局）的文档检索时，现代多模态检索器（如 ColPali, ColQwen2）表现远超传统的词法检索（如 BM25）。学术界普遍认为这是由于检索算法的代际进步。
*   **核心问题**：作者质疑这种性能差距是否真的源于“检索机制”本身，还是仅仅因为传统的文本检索器受限于劣质的 OCR（光学字符识别）转录和不足的文本预处理。
*   **研究意义**：通过重新评估，旨在揭示“表示（Representation）”与“检索（Retrieval）”在 RAG 性能提升中的真实贡献占比。

### 2. 方法论
*   **核心思想**：采用**控制变量法**。在保持检索算法（BM25）完全不变的前提下，系统性地升级文档的“表示层”，即改变 OCR 转录质量和语言特定的预处理流程。
*   **关键技术细节**：
    *   **多级 OCR 方案**：对比了四种方案：Adobe PDF 提取（默认）、EasyOCR（开源）、Mistral OCR 3（现代专用 OCR）、以及 Ministral 3B（利用小规模视觉语言模型 VLM 进行 Markdown 转录）。
    *   **语言特定预处理**：针对不同语系实施定制化处理，包括罗曼语系的词干提取（Stemming）、高度屈折语（如捷克语）的词形还原（Lemmatization）、阿拉伯语的形态分析（Morphological analysis）以及日语的分词（Segmentation）。
    *   **语义增强**：对于图表等视觉元素，利用 VLM 生成简短的语义描述，将其注入文本索引中。

### 3. 实验设计
*   **数据集**：**VisR-Bench**。这是一个专门用于评估视觉丰富、长文档、多语言 RAG 的基准测试。
*   **实验场景**：
    1.  **多语言检索**：涵盖 15 种语言（包括西班牙语、阿拉伯语、日语、越南语等）。
    2.  **视觉丰富检索**：专门针对包含大量图表（Figure）和表格（Table）的页面。
*   **对比方法（Baseline）**：
    *   **词法检索**：基础 BM25。
    *   **稠密文本检索**：SBERT, BGE-large, BGE-M3, NV-Embed-v2。
    *   **多模态检索**：CLIP, SigLip, VisRAG, VLM2Vec, GME, 以及 Col* 系列（ColPali, ColQwen2 等）。
*   **评价指标**：Top-1 和 Top-5 检索准确率（Accuracy）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究的核心是评估和推理（使用现有的 OCR 模型和检索器进行消融实验），而非从头训练大型模型，因此对算力的需求主要集中在对 VisR-Bench 数据集的批量推理上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 15 种语言中的每一种都进行了详尽的 OCR 模型对比和预处理策略消融（见论文 Table 3）。
    *   针对图表、表格、纯文本三种文档类型分别进行了性能拆解。
*   **充分性评价**：实验设计非常**充分且客观**。作者不仅展示了 BM25 的提升，还证明了改进 OCR 同样能提升 SBERT 和 BGE-M3 等稠密检索器的性能，这增强了“表示层是瓶颈”这一结论的普适性。

### 6. 主要结论与发现
*   **表示层是主因**：通过改进 OCR 和预处理，BM25 在多语言基准上的 Top-5 准确率提升了 **8.9%**，在图表密集型文档上提升了高达 **31.1%**。
*   **BM25 依然能打**：优化后的 BM25（BM25+OCR）在多语言平均表现上超越了许多现代稠密检索器（如 BGE-M3, NV-Embed-v2），并大幅缩小了与顶级多模态模型（如 ColPali）的差距。
*   **瓶颈识别**：
    *   在多语言场景中，形态复杂的语言（如阿拉伯语）主要受限于预处理；而日语、越南语等则主要受限于 OCR 质量。
    *   在视觉场景中，缺失图表内的文本标签（如坐标轴、图例）是导致传统检索失败的主因。

### 7. 优点
*   **批判性思维**：挑战了当前“多模态检索器在算法上具有绝对代差”的流行叙事，回归到数据质量这一本质问题。
*   **实用性强**：为工业界提供了一个低成本提升 RAG 性能的路径——与其更换昂贵的多模态检索模型，不如优化 OCR 和语言预处理。
*   **分解式评估**：提出了将“转录能力”与“检索能力”解耦评估的必要性，对未来 Benchmark 的设计具有指导意义。

### 8. 不足与局限
*   **视觉推理上限**：尽管优化后的 BM25 表现强劲，但在处理**纯视觉逻辑**（如空间关系、非文本编码的图形趋势）时，依然无法逾越与多模态模型（如 ColQwen2）之间的鸿沟。
*   **预处理复杂性**：虽然证明了语言特定预处理的有效性，但在实际大规模应用中，为每种语言维护一套复杂的 NLP 管道（如词形还原、形态分析）会增加系统维护成本。
*   **实验覆盖**：研究主要集中在检索阶段（Retrieval），未深入探讨表示层的改进对后续生成阶段（Generation）的具体量化影响。

（完）
