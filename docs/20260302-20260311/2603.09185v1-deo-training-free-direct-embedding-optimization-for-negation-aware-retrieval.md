---
title: "DEO: Training-Free Direct Embedding Optimization for Negation-Aware Retrieval"
title_zh: DEO：面向否定感知检索的免训练直接嵌入优化
authors: "Taegyeong Lee, Jiwon Park, Seunghyun Hwang, JooYoung Jang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09185v1"
tags: ["query:rag"]
score: 9.0
evidence: 针对否定感知RAG的直接嵌入优化
tldr: 针对现有检索方法在处理否定和排除查询时表现不佳且微调成本高的问题，本文提出了一种无需训练的直接嵌入优化（DEO）方法。DEO通过将查询分解为正向和负向组件，并利用对比目标优化查询嵌入，在文本和多模态检索任务中显著提升了性能，为现实场景提供了一种高效且实用的否定感知检索方案。
selection_source: fresh_fetch
motivation: 现有的检索方法难以准确处理否定和排除类查询，且现有的改进方案通常需要高昂的微调成本。
method: 提出DEO方法，将查询分解为正负组件，并在无需训练的情况下通过对比目标直接优化查询嵌入。
result: "在NegConstraint基准测试中显著超越基准模型，并在多模态检索中将OpenAI CLIP的Recall@5提升了6%。"
conclusion: DEO是一种高效、无需训练的否定感知检索方法，具有极强的实际应用价值。
---

## 摘要
大语言模型 (LLMs) 和检索增强生成 (RAG) 的最新进展催生了多种检索方法。然而，现有的检索方法在处理否定和排除查询时往往难以准确检索结果。为了解决这一局限性，先前的方法通常依赖于嵌入适配或微调，这会引入额外的计算成本和部署复杂性。我们提出了直接嵌入优化 (DEO)，这是一种用于否定感知文本和多模态检索的免训练方法。DEO 将查询分解为正向和负向组件，并使用对比目标来优化查询嵌入。在无需额外训练数据或模型更新的情况下，DEO 在 NegConstraint 基准测试上的表现优于基线模型，nDCG@10 提升了 0.0738，MAP@100 提升了 0.1028，同时在多模态检索中相比 OpenAI CLIP 将 Recall@5 提高了 6%。这些结果证明了 DEO 在现实世界否定和排除感知检索场景中的实用性。

## Abstract
Recent advances in Large Language Models (LLMs) and Retrieval-Augmented Generation (RAG) have enabled diverse retrieval methods. However, existing retrieval methods often fail to accurately retrieve results for negation and exclusion queries. To address this limitation, prior approaches rely on embedding adaptation or fine-tuning, which introduce additional computational cost and deployment complexity. We propose Direct Embedding Optimization (DEO), a training-free method for negation-aware text and multimodal retrieval. DEO decomposes queries into positive and negative components and optimizes the query embedding with a contrastive objective. Without additional training data or model updates, DEO outperforms baselines on NegConstraint, with gains of +0.0738 nDCG@10 and +0.1028 MAP@100, while improving Recall@5 by +6\% over OpenAI CLIP in multimodal retrieval. These results demonstrate the practicality of DEO for negation- and exclusion-aware retrieval in real-world settings.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DEO (Direct Embedding Optimization)** 的新方法，旨在解决密集检索模型在处理包含“否定”或“排除”逻辑的查询时表现不佳的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的密集检索（Dense Retrieval）模型（如 BGE、CLIP 等）虽然在语义匹配上表现优异，但往往难以理解查询中的**否定逻辑**（例如：“给我看财报，但排除 2024 年的结果”）。
*   **研究背景**：
    *   传统的解决方法通常需要对模型进行全量微调或添加适配器（Adapter），这需要大量的标注数据和昂贵的 GPU 算力。
    *   现有的非参数化方法（如 NUDGE）虽然减少了训练开销，但仍需针对特定数据集进行优化。
*   **研究目标**：开发一种**免训练（Training-Free）**、**模型无关（Model-Agnostic）**且能精准处理否定逻辑的检索增强方法。

### 2. 提出的方法论：DEO (直接嵌入优化)
DEO 的核心思想是在推理阶段通过优化“查询向量”本身，使其在嵌入空间中远离负向语义、靠近正向语义。其流程分为两个阶段：

1.  **查询分解 (Query Decomposition)**：
    *   利用大语言模型（LLM，如 GPT-4）将原始查询分解为**正向子查询**（用户想要的内容）和**负向子查询**（用户想要排除的内容）。
    *   例如：“排除 2024 年的财报”会被分解为正向（“2025 年财报”、“财务报表”）和负向（“2024 年收益”、“2024 年财务报告”）。
2.  **直接嵌入优化 (Direct Embedding Optimization)**：
    *   **初始化**：将原始查询的嵌入向量作为初始值。
    *   **对比损失函数**：定义一个包含三部分的损失函数：
        *   **吸引项**：拉近优化向量与正向子查询向量的距离。
        *   **排斥项**：推开优化向量与负向子查询向量的距离。
        *   **一致性项**：确保优化后的向量不会偏离原始查询太远，以保持基本语义。
    *   **迭代更新**：使用 Adam 优化器在推理时对该向量进行少量步数（通常 20-50 步）的梯度下降，而保持预训练模型（Encoder）权重冻结。

### 3. 实验设计
*   **数据集**：
    *   **文本检索**：NegConstraint（否定约束基准）、NevIR（否定神经检索基准）。
    *   **多模态检索**：COCO-Neg（包含否定描述的图文检索）。
*   **评估指标**：nDCG@10、MAP@100、Pairwise 准确率、Recall@5。
*   **对比基线**：
    *   **文本**：BGE-small/large-en-v1.5, BGE-M3。
    *   **多模态**：OpenAI CLIP, CLIP-laion400m, CLIP-datacomp, 以及专门针对否定优化的 NegCLIP。

### 4. 资源与算力
*   **硬件环境**：论文明确提到了测试环境，包括 CPU (AMD Ryzen 7 5800X) 和 GPU (NVIDIA GeForce RTX 3060 12GB)。
*   **计算开销**：
    *   DEO 非常高效。在 GPU 上，20 步优化平均仅需 **0.033 秒/查询**；在 CPU 上仅需 **0.016 秒/查询**。
    *   由于不需要微调模型权重，该方法对显存要求极低，适合资源受限的部署环境。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从小型（BGE-small）到大型（BGE-M3）以及多模态（CLIP）的多种模型。
    *   进行了详细的**消融实验**，探讨了不同 LLM（GPT-4 vs Qwen-1.5B）、不同超参数（$\lambda$ 权重）、优化步数以及“仅分解不优化”的效果对比。
*   **客观性与公平性**：实验在标准公开基准上进行，对比了当前最先进的开源模型，并展示了 DEO 在不同架构模型上均有稳定的增益，证明了其普适性。

### 6. 主要结论与发现
*   **性能显著提升**：在 NegConstraint 任务中，BGE-large 模型应用 DEO 后，MAP 提升了 **16.32%**，nDCG@10 提升了 **10.34%**。
*   **多模态有效性**：在 COCO-Neg 任务中，OpenAI CLIP 的 Recall@5 提升了 **6%**。即使是已经针对否定做过微调的 NegCLIP，应用 DEO 后仍有进一步提升。
*   **优化优于分解**：实验证明，单纯靠 LLM 分解查询并进行简单的向量平均或 RRF 融合，效果远不如 DEO 的对比优化过程。
*   **可视化验证**：通过 PCA 轨迹分析，观察到查询向量在优化过程中确实向正向样本移动并远离了负向干扰项。

### 7. 优点与亮点
*   **免训练 (Training-Free)**：无需更新模型参数，无需收集大规模负样本数据集，即插即用。
*   **极高的灵活性**：模型无关（Model-Agnostic）且模态无关（Modality-Agnostic），适用于任何能产生嵌入向量的检索系统。
*   **高效性**：推理时的微小延迟换取了巨大的准确率提升，且在 CPU 上也能流畅运行。
*   **可解释性**：通过调整正负向权重，开发者可以直观地控制检索系统对“排除项”的敏感程度。

### 8. 不足与局限
*   **依赖 LLM 的分解质量**：DEO 的效果高度依赖于第一步 LLM 分解的准确性。如果 LLM 误解了否定逻辑，优化方向可能会产生偏差。
*   **推理延迟增加**：虽然单次优化仅需几十毫秒，但在极高并发的实时检索场景下，LLM 调用和梯度优化步骤仍会带来额外的延迟。
*   **超参数敏感性**：虽然论文给出了推荐配置，但在不同领域的数据集上，可能仍需微调正负向权重的比例（$\lambda$）以达到最优效果。

（完）
