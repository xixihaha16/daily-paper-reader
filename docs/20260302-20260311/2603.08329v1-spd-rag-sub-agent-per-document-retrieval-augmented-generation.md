---
title: "SPD-RAG: Sub-Agent Per Document Retrieval-Augmented Generation"
title_zh: SPD-RAG：基于每文档子代理的检索增强生成
authors: "Yagiz Can Akay, Muhammed Yusuf Kartal, Esra Alparslan, Faruk Ortakoyluoglu, Arda Akpinar"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08329v1"
tags: ["query:rag"]
score: 10.0
evidence: 跨文档RAG的分层多智能体框架
tldr: 针对复杂跨文档问答中标准RAG覆盖不足及长文本模型推理难的问题，本文提出SPD-RAG框架。该框架采用分层多智能体架构，为每个文档分配专用子智能体进行局部检索，由协调器统一调度并聚合结果。通过递归Map-Reduce合成答案，在提升问答质量与可扩展性的同时，显著降低了API成本，为大规模语料库的高效检索增强生成提供了新方案。
selection_source: fresh_fetch
motivation: 传统RAG在处理分散于海量文档中的复杂事实时存在证据覆盖不全的问题，而长文本模型在超大规模输入下推理可靠性不足。
method: 提出一种按文档分解任务的层次化多智能体框架，每个文档由独立智能体处理，再通过协调器与令牌受限的合成层进行结果融合。
result: "在LOONG基准测试中，SPD-RAG的平均得分（58.1）远超普通RAG（33.0），且成本仅为全上下文基线的38%。"
conclusion: SPD-RAG通过文档级专业化与集中融合，实现了在异构多文档环境下高效、低成本且高质量的复杂问答。
---

## 摘要
回答复杂的现实世界查询通常需要综合散布在海量文档语料库中的事实。在这些场景下，标准的检索增强生成 (RAG) 流水线面临证据覆盖不全的问题，而长上下文大语言模型 (LLM) 在处理海量输入时难以进行可靠的推理。我们提出了 SPD-RAG，这是一种用于详尽跨文档问答的分层多代理框架，它沿文档轴对问题进行分解。每个文档由一个专门的文档级代理处理，该代理仅针对其自身内容运行，从而实现聚焦检索；同时，协调器将任务分派给相关代理并汇总其部分答案。代理的输出通过一个受 Token 限制的合成层合并部分答案来进行综合（该层支持针对海量语料库的递归 Map-Reduce）。这种具有集中融合功能的文档级专业化提高了异构多文档设置下的可扩展性和回答质量，同时构建了一个模块化、可扩展的检索流水线。在用于长上下文多文档问答的 LOONG 基准测试（EMNLP 2024）中，SPD-RAG 获得了 58.1 的平均分（GPT-5 评估），优于普通 RAG (33.0) 和代理式 RAG (32.8)，而其 API 成本仅为全上下文基线 (68.0) 的 38%。

## Abstract
Answering complex, real-world queries often requires synthesizing facts scattered across vast document corpora. In these settings, standard retrieval-augmented generation (RAG) pipelines suffer from incomplete evidence coverage, while long-context large language models (LLMs) struggle to reason reliably over massive inputs. We introduce SPD-RAG, a hierarchical multi-agent framework for exhaustive cross-document question answering that decomposes the problem along the document axis. Each document is processed by a dedicated document-level agent operating only on its own content, enabling focused retrieval, while a coordinator dispatches tasks to relevant agents and aggregates their partial answers. Agent outputs are synthesized by merging partial answers through a token-bounded synthesis layer (which supports recursive map-reduce for massive corpora). This document-level specialization with centralized fusion improves scalability and answer quality in heterogeneous multidocument settings while yielding a modular, extensible retrieval pipeline. On the LOONG benchmark (EMNLP 2024) for long-context multi-document QA, SPD-RAG achieves an Avg Score of 58.1 (GPT-5 evaluation), outperforming Normal RAG (33.0) and Agentic RAG (32.8) while using only 38% of the API cost of a full-context baseline (68.0).

---

## 论文详细总结（自动生成）

### SPD-RAG：基于每文档子代理的检索增强生成论文总结

#### 1. 论文的核心问题与整体含义
在处理复杂的现实世界查询时，往往需要从大量异构文档中综合信息。现有的解决方案存在两大瓶颈：
*   **标准 RAG 的覆盖率不足**：传统的 Top-K 检索会丢弃排名靠后的文档，导致散布在多个文档中的关键证据被遗漏。
*   **长上下文 LLM 的推理退化**：虽然模型窗口在扩大，但随着输入长度增加，模型在海量信息中进行可靠推理的能力会显著下降（即“迷失在中间”现象）。
**SPD-RAG** 的核心目标是实现“详尽的跨文档问答”，确保不遗漏任何相关文档中的证据，同时保持推理的高效性和低成本。

#### 2. 论文提出的方法论
SPD-RAG 采用了一种**分层多智能体架构**，将问题沿“文档轴”进行分解，主要分为三个层级：
*   **协调层 (Coordination Layer)**：由一个高级协调器（如 Gemini 2.5 Pro）将用户查询分解为“共享指令集”（具体的提取任务）和“合成指令”（指导后续如何汇总）。
*   **并行检索层 (Parallel Retrieval Layer)**：为语料库中的**每一个文档**分配一个专用的子代理（使用成本更低的 Gemini 2.5 Flash）。每个子代理在各自独立的文档空间内运行 RAG 循环，进行多次搜索和推理，提取与指令相关的原始事实。这种做法避免了跨文档的干扰。
*   **合成层 (Synthesis Layer)**：采用**递归 Map-Reduce 机制**。
    *   **相似性排序合并**：利用聚类算法（凝聚层次聚类）将语义相似的代理输出分组。
    *   **令牌受限合成**：在预设的 Token 预算（如 75 万）内，递归地合并这些分组，直到生成最终答案。这种设计理论上可以支持无限规模的语料库。

#### 3. 实验设计
*   **数据集/基准测试**：使用了 **LOONG 基准测试**（EMNLP 2024），该测试专门针对长上下文多文档问答，要求“不留下任何文档”（所有文档都与答案相关）。
*   **场景**：涵盖金融报告和学术论文，上下文长度在 20k 到 250k Token 之间。
*   **任务类型**：聚光灯定位（Spotlight Locating）、对比（Comparison）、聚类（Clustering）、推理链（Chain of Reasoning）。
*   **对比方法**：
    1.  **全上下文基线 (Full Context)**：将所有文档拼接后直接输入模型（作为性能上限）。
    2.  **普通 RAG (Normal RAG)**：标准的 Top-K 向量检索。
    3.  **代理式 RAG (Agentic RAG)**：单个智能体在全局索引上进行迭代检索。

#### 4. 资源与算力
论文**未明确说明**具体的本地 GPU 型号或训练时长。由于该研究主要基于商业 API 构建：
*   **模型使用**：协调和合成层使用 **Gemini 2.5 Pro**，文档子代理使用 **Gemini 2.5 Flash**。
*   **向量数据库**：使用 **Qdrant** 进行索引。
*   **嵌入与重排序**：使用 **Cohere** 的 `embed-v4.0` 和 `rerank-v4.0-fast`。
*   **评估**：使用 **GPT-5** 作为自动评分法官。

#### 5. 实验数量与充分性
*   **实验规模**：共评估了 102 个实例（40 篇学术论文，62 份金融报告）。
*   **充分性**：实验涵盖了不同任务难度和文档领域，并进行了详细的成本-质量权衡分析。
*   **客观性**：通过重新计算基线模型在相同评估协议（GPT-5 Judge）下的得分，确保了对比的公平性。

#### 6. 论文的主要结论与发现
*   **性能卓越**：SPD-RAG 的平均得分（58.1）远高于普通 RAG（33.0）和代理式 RAG（32.8）。
*   **成本效益高**：SPD-RAG 达到了全上下文基线 85.4% 的质量，但 API 成本仅为其 **37.9%**。
*   **擅长复杂任务**：在需要综合多方证据的“聚类”和“推理链”任务中，SPD-RAG 的提升最为显著（比普通 RAG 高出约 40 分）。
*   **解决学术论文难题**：普通 RAG 在学术论文场景下的完美率几乎为 0，而 SPD-RAG 能够有效恢复提取能力。

#### 7. 优点
*   **架构创新**：将任务按文档维度拆分，实现了天然的并行化，并解决了全局检索时的信息干扰问题。
*   **递归合成设计**：通过相似性聚类进行递归合并，使系统具备处理超大规模语料库的潜力。
*   **成本优化**：巧妙地在底层使用廉价模型（Flash），在高层使用强大模型（Pro），实现了极佳的性价比。

#### 8. 不足与局限
*   **延迟较高**：由于需要经过协调、并行检索、合成等多个串行步骤，端到端延迟（约 55 秒）高于单次调用的基线。
*   **依赖协调器质量**：如果协调层生成的子任务描述不准确，会导致底层子代理提取信息不全。
*   **递归路径未充分验证**：在 LOONG 测试中，由于 Gemini 的窗口足够大，递归合成路径并未被多次触发，其在数千个文档规模下的稳定性仍需进一步验证。
*   **性能差距**：尽管优于传统 RAG，但距离全上下文基线仍有约 10 分的差距。

（完）
