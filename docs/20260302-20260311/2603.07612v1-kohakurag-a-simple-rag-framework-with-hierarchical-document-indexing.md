---
title: "KohakuRAG: A simple RAG framework with hierarchical document indexing"
title_zh: KohakuRAG：一种具有分层文档索引的简单 RAG 框架
authors: "Shih-Ying Yeh, Yueh-Feng Ku, Ko-Wei Huang, Buu-Khang Tu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07612v1"
tags: ["query:rag"]
score: 10.0
evidence: 具有文档索引的分层RAG框架
tldr: KohakuRAG是一个针对高精度引用需求设计的层次化RAG框架。它通过四层树状结构（文档-章节-段落-句子）保留文档结构，利用LLM查询规划器提高检索覆盖率，并采用集成推理与弃权感知投票来稳定输出。在WattBot 2025挑战赛中，该框架凭借卓越的数值精度和准确的归因能力获得双榜冠军，证明了层次化索引和集成策略在复杂技术问答中的有效性。
selection_source: fresh_fetch
motivation: 传统RAG系统在处理高精度引用需求时，存在文档结构丢失、检索覆盖率不足以及单次推理结果不稳定等问题。
method: 该框架采用四层树状索引结构进行自下而上的嵌入聚合，并结合LLM查询规划、跨查询重排序及集成推理投票机制。
result: KohakuRAG在WattBot 2025挑战赛中获得第一名（得分0.861），且消融实验显示提示词排序和重试机制对性能贡献巨大。
conclusion: 层次化索引与集成推理的结合能有效解决复杂文档问答中的精度与稳定性难题，且该框架已作为开源软件发布。
---

## 摘要
检索增强生成（RAG）系统在从文档集合中回答问题时，若要求高精度引用，则面临多重困难：扁平化分块策略牺牲了文档结构，单查询表述因词汇不匹配而遗漏相关段落，且单次推理产生的随机答案在内容和引用选择上均存在波动。我们提出了 KohakuRAG，这是一个分层 RAG 框架，它通过四层树状表示（文档 $\rightarrow$ 章节 $\rightarrow$ 段落 $\rightarrow$ 句子）及自底向上的嵌入聚合来保留文档结构，通过基于大语言模型（LLM）的查询规划器和跨查询重排序来提高检索覆盖率，并通过带有弃权感知投票的集成推理来稳定答案。我们在 WattBot 2025 挑战赛上进行了评估，该基准测试要求系统根据 32 份文档回答技术问题，数值容差为 $\pm$0.1%，并要求精确的来源归属。KohakuRAG 在公开和私有排行榜上均获得第一名（最终得分为 0.861），是唯一在两个评估分区中均保持领先地位的团队。消融研究表明，提示词排序（相对提升 +80%）、重试机制（+69%）以及带有空白过滤的集成投票（+1.2 个百分点）均做出了实质性贡献，而仅靠分层稠密检索即可与混合稀疏-稠密方法相媲美（BM25 仅增加 +3.1 个百分点）。我们将 KohakuRAG 作为开源软件发布，网址为 https://github.com/KohakuBlueleaf/KohakuRAG。

## Abstract
Retrieval-augmented generation (RAG) systems that answer questions from document collections face compounding difficulties when high-precision citations are required: flat chunking strategies sacrifice document structure, single-query formulations miss relevant passages through vocabulary mismatch, and single-pass inference produces stochastic answers that vary in both content and citation selection. We present KohakuRAG, a hierarchical RAG framework that preserves document structure through a four-level tree representation (document $\rightarrow$ section $\rightarrow$ paragraph $\rightarrow$ sentence) with bottom-up embedding aggregation, improves retrieval coverage through an LLM-powered query planner with cross-query reranking, and stabilizes answers through ensemble inference with abstention-aware voting. We evaluate on the WattBot 2025 Challenge, a benchmark requiring systems to answer technical questions from 32 documents with $\pm$0.1% numeric tolerance and exact source attribution. KohakuRAG achieves first place on both public and private leaderboards (final score 0.861), as the only team to maintain the top position across both evaluation partitions. Ablation studies reveal that prompt ordering (+80% relative), retry mechanisms (+69%), and ensemble voting with blank filtering (+1.2pp) each contribute substantially, while hierarchical dense retrieval alone matches hybrid sparse-dense approaches (BM25 adds only +3.1pp). We release KohakuRAG as open-source software at https://github.com/KohakuBlueleaf/KohakuRAG.

---

## 论文详细总结（自动生成）

这是一份关于论文《KohakuRAG: A simple RAG framework with hierarchical document indexing》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对检索增强生成（RAG）系统在处理**高精度技术问答**和**精确引用归属**时的三大核心痛点：
*   **结构丢失**：传统的扁平化分块（Flat Chunking）破坏了文档的逻辑层级，导致难以实现精确到句子或段落的引用。
*   **词汇鸿沟**：单一查询往往无法覆盖文档中不同表述的相关内容（如缩写与全称的匹配）。
*   **推理不稳定性**：大语言模型（LLM）的单次推理具有随机性，容易产生幻觉或在证据存在时错误地选择弃权。
*   **背景**：该研究以 **WattBot 2025 挑战赛**为背景，该比赛要求对 32 份技术文档进行问答，数值精度要求极高（±0.1%），且必须提供精确的来源引用。

### 2. 方法论：核心思想与关键技术
KohakuRAG 提出了一个包含三个阶段的层次化框架：

*   **分层文档索引（Hierarchical Indexing）**：
    *   **四层树状结构**：将文档解析为“文档 $\rightarrow$ 章节 $\rightarrow$ 段落 $\rightarrow$ 句子”的树状模型。
    *   **自底向上聚合**：先计算叶子节点（句子）的嵌入，再通过长度加权平均（Length-weighted aggregation）计算父节点（段落、章节）的嵌入。公式为：$e_v = \frac{\sum_{c \in C(v)} w_c \cdot e_c}{\sum w_c}$，其中 $w_c$ 为子节点的 token 数。
*   **多查询检索与跨查询重排序**：
    *   **查询规划器**：利用 LLM 将原始问题扩展为 $n$ 个语义相关的查询（如重写、缩写展开、分解子问题）。
    *   **跨查询重排序（Cross-Query Reranking）**：综合考虑节点被检索到的**频率**和**相似度分数**进行排序，优先展示被多个查询共同命中的节点。
*   **集成推理与弃权感知投票**：
    *   **提示词优化**：发现将上下文置于问题之前（C $\rightarrow$ Q）能显著提升性能（缓解“迷失在中间”现象）。
    *   **重试机制**：若模型输出“证据不足（is_blank）”，则自动增加检索深度 $k$ 并重新尝试。
    *   **集成投票**：运行 $m$ 次独立推理，在投票前过滤掉空白响应，通过多数投票确定最终答案和引用集合。

### 3. 实验设计
*   **数据集/场景**：WattBot 2025 Challenge。包含 32 份关于 AI 能源消耗的技术文档（约 50 万 token），涉及电力、水耗、碳足迹等。
*   **Benchmark**：官方评分标准（75% 数值准确度 + 15% 引用精确度 + 10% 幻觉规避）。
*   **对比方法**：
    *   **嵌入模型**：对比了 Jina v3（纯文本）和 Jina v4（多模态/图像）。
    *   **生成模型**：横向对比了 GPT-5 系列、Gemini 3 Pro、Grok 4.1 Fast、Mistral Large、Kimi-k2 等主流模型。
    *   **检索策略**：对比了纯稠密检索、BM25 混合检索、不同重排序策略。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **推理支持**：文中提到 Comfy Org 资助了 LLM 的推理成本。由于该框架主要基于现成的嵌入模型和 API 进行推理，不涉及大规模模型预训练，因此算力消耗主要集中在多路并发推理（集成推理阶段）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了详尽的消融实验，涵盖提示词顺序、重试机制、集成规模（$n=1$ 到 $15$）、检索深度 $k$ 等。
    *   对 **2,583 次预测**进行了深入的错误分析，将错误细分为 7 类（如不必要弃权、引用不匹配、数值选择错误等）。
*   **充分性与客观性**：实验非常充分。作者不仅在训练集上做了大量测试，还在 WattBot 2025 的公开（Public）和私有（Private）榜单上均验证了结果。作为唯一一个在两个榜单均保持第一的团队，其结果具有很强的说服力和鲁棒性。

### 6. 主要结论与发现
*   **提示词顺序至关重要**：将上下文放在问题之前，相对性能提升了 **80%**。
*   **重试机制有效性**：在低检索深度下，重试机制使性能提升了 **69%**，有效解决了模型因证据难寻而保守弃权的问题。
*   **分层检索的优势**：仅靠分层稠密检索就表现优异，加入 BM25 仅带来 **3.1pp** 的微小提升，说明结构化信息比简单的关键词匹配更重要。
*   **集成策略的稳定性**：集成推理能显著降低单次推理的随机性，且“过滤空白答案”的策略对最终得分贡献巨大。

### 7. 优点
*   **结构化思维**：通过树状索引优雅地解决了 RAG 中常见的上下文断裂和引用归属模糊问题。
*   **鲁棒性强**：集成推理和重试机制使其在面对分布偏移（私有榜单测试）时表现极其稳定。
*   **多模态支持**：原生支持图像标题检索和视觉模型直接输入，适应现代技术文档。
*   **开源贡献**：代码完全开源，具有很高的实用价值。

### 8. 不足与局限
*   **解析依赖**：文档解析依赖于基于规则的启发式方法（如字体大小判断标题），对于排版极其复杂的文档（如多栏、非标准层级）可能失效。
*   **成本与延迟**：集成推理（如运行 15 次）虽然提升了精度，但也带来了线性增长的 API 成本和响应延迟。
*   **引用偏差**：错误分析显示，模型有时会倾向于引用综述性论文而非原始数据来源，引用精度仍有提升空间。
*   **领域局限**：目前仅在 AI 能源这一特定技术领域验证，在更大规模语料库或非技术领域的泛化性有待进一步观察。

（完）
