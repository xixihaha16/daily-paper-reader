---
title: "Retrieval, Refinement, and Ranking for Text-to-Video Generation via Prompt Optimization and Test-Time Scaling"
title_zh: 通过提示词优化与测试时缩放实现文本到视频生成的检索、细化与排序
authors: "Zillur Rahman, Alex Sheng, Cristian Meo"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01509v1"
tags: ["query:rag"]
score: 9.0
evidence: 基于RAG的提示词优化框架
tldr: 针对文本生成视频（T2V）模型对提示词高度敏感且微调成本高的问题，本文提出 3R 框架。该框架无需模型训练，通过检索增强生成（RAG）提取修饰词、基于扩散模型的偏好优化以及时间帧插值，实现了提示词的自动优化与推理时扩展。实验证明 3R 显著提升了生成视频的静态保真度与动态连贯性，为高质量视频生成提供了新思路。
selection_source: fresh_fetch
motivation: 现有的 T2V 优化方法通常依赖复杂的后期编辑或昂贵的模型微调，难以兼顾生成质量与计算效率。
method: 提出一种基于 RAG 的提示词优化框架，通过修饰词提取、偏好优化对齐和时间帧插值来增强生成效果。
result: 实验证明 3R 框架在无需训练的情况下，显著提升了生成视频的静态视觉保真度和动态时空连贯性。
conclusion: 通过在推理侧进行提示词优化和计算扩展，3R 为提升 T2V 模型的生成表现提供了一种高效且通用的解决方案。
---

## 摘要
尽管大规模数据集推动了文本到视频（T2V）生成模型的显著进展，但这些模型对输入提示词仍高度敏感，这表明提示词设计对生成质量至关重要。当前改进视频输出的方法往往存在不足：它们要么依赖复杂的后期编辑模型，面临引入伪影的风险；要么需要对核心生成器进行昂贵的微调，这严重限制了可扩展性和可访问性。在这项工作中，我们提出了 3R，一种基于检索增强生成（RAG）的新型提示词优化框架。3R 利用了当前最先进的 T2V 扩散模型和视觉语言模型的能力。它可以在不进行任何模型训练的情况下与任何 T2V 模型配合使用。该框架利用了三个关键策略：基于 RAG 的修饰符提取以增强上下文关联，基于扩散的偏好优化以使输出符合人类偏好，以及时间帧插值以产生时间一致的视觉内容。这些组件共同实现了更准确、高效且上下文对齐的文本到视频生成。实验结果证明了 3R 在增强生成视频的静态保真度和动态连贯性方面的有效性，强调了优化用户提示词的重要性。

## Abstract
While large-scale datasets have driven significant progress in Text-to-Video (T2V) generative models, these models remain highly sensitive to input prompts, demonstrating that prompt design is critical to generation quality. Current methods for improving video output often fall short: they either depend on complex, post-editing models, risking the introduction of artifacts, or require expensive fine-tuning of the core generator, which severely limits both scalability and accessibility. In this work, we introduce 3R, a novel RAG based prompt optimization framework. 3R utilizes the power of current state-of-the-art T2V diffusion model and vision language model. It can be used with any T2V model without any kind of model training. The framework leverages three key strategies: RAG-based modifiers extraction for enriched contextual grounding, diffusion-based Preference Optimization for aligning outputs with human preferences, and temporal frame interpolation for producing temporally consistent visual contents. Together, these components enable more accurate, efficient, and contextually aligned text-to-video generation. Experimental results demonstrate the efficacy of 3R in enhancing the static fidelity and dynamic coherence of generated videos, underscoring the importance of optimizing user prompts.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **3R**（Retrieval, Refinement, and Ranking）的框架，旨在通过提示词优化和推理时计算缩放（Test-Time Scaling）来提升文本到视频（T2V）生成的质量。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的 T2V 模型对输入提示词（Prompt）高度敏感。用户提供的短提示词往往缺乏细节，导致生成视频质量不稳定。
*   **核心挑战**：现有的改进方法要么依赖复杂的后期编辑（易产生伪影），要么需要昂贵的模型微调（扩展性差）。
*   **整体含义**：论文提出了一种**无需训练（Training-free）**、**黑盒适用（Black-box applicable）**的即插即用框架。它通过在推理阶段增加计算量（如生成多个候选视频并排序），将提示词优化与检索增强生成（RAG）相结合，显著提升了现有开源 T2V 模型的表现。

### 2. 方法论：3R 框架
3R 框架由三个核心阶段组成：
*   **检索（Retrieval）**：
    *   利用预训练的 Sentence Transformer 将用户原始提示词与关系数据库中的场景修饰符（Subject, Action, Environment）进行匹配。
    *   通过余弦相似度筛选出最相关的修饰符，为原始提示词提供丰富的上下文背景。
*   **细化（Refinement）**：
    *   **合并**：使用大语言模型（LLM，如 Mistral）将检索到的修饰符迭代合并到原始提示词中。
    *   **变体生成**：利用 LLM（如 GPT-4o）生成 $N$ 个既保持用户初衷又具有细节差异的精细化提示词。
*   **排序与增强（Ranking & Enhancement）**：
    *   **生成与排序**：将 $N$ 个提示词输入 T2V 基础模型，生成 $N$ 个候选视频。使用基于视觉问答（VQA）的奖励模型（VisionReward）对视频进行评分，选出最优候选。
    *   **时间插值**：对选中的视频进行时间帧插值（从 16 帧扩展到 61 帧），利用预训练的 UNet 扩散模型增强视频的运动平滑度和时间连贯性。

### 3. 实验设计
*   **数据集/场景**：使用了 **EvalCrafter** 基准测试，这是目前最全面的 T2V 评估标准。
*   **评估指标**：涵盖 17 个维度，最终聚合为四个类别：文本-视频对齐（Alignment）、视觉质量（Visual Quality）、运动质量（Motion Quality）和时间连贯性（Temporal Consistency）。
*   **对比方法**：对比了 Show-1、LaVie（作为 3R 的基础模型）、IPO 和 Videocrafter2 等开源最先进模型。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **Nvidia H200 GPU**。
*   **推理效率**：在使用 LaVie 作为基础模型时，生成单个视频大约需要 **5 秒**。
*   **算力消耗**：由于 3R 涉及生成 $N$ 个候选视频（实验中 $N=4$）以及 LLM 的多次调用和时间插值，其推理成本显著高于单次生成的基准模型，这体现了“以推理时间换质量”的策略。

### 5. 实验数量与充分性
*   **实验规模**：论文在 EvalCrafter 榜单上进行了完整的定量对比，并提供了多组定性视觉对比（如处理复杂语义、虚构角色和文字生成）。
*   **消融实验**：设计了 5 组消融实验，分别验证了 RAG 增强、多候选排序、时间插值以及 VLM 反馈（Critique）对最终得分的影响。
*   **充分性评价**：实验设计较为客观，通过对比基础模型（LaVie）在加入 3R 框架前后的表现，清晰地证明了框架的增益。但实验主要集中在开源模型上，未与 Sora 等闭源顶尖模型直接对比。

### 6. 主要结论与发现
*   **性能领先**：3R 在 EvalCrafter 基准测试中取得了总分第一（245分），超越了现有的开源 SOTA 模型。
*   **推理缩放有效性**：增加推理时的计算量（如提示词细化和多候选采样）是弥补基础模型架构能力不足的有效途径。
*   **VLM 反馈瓶颈**：实验发现，引入视觉语言模型（VLM）进行自动评价和反馈（Critique）目前效果有限，容易出现语义漂移或过度纠错，导致性能反而下降。

### 7. 优点
*   **通用性强**：作为黑盒框架，可以适配任何现有的 T2V 模型（如 LaVie, Wan 等）。
*   **无需训练**：避免了昂贵的 GPU 训练成本，适合资源有限的开发者。
*   **多维度提升**：不仅优化了静态的文本对齐，还通过插值技术解决了视频生成中常见的闪烁和不连贯问题。

### 8. 不足与局限
*   **推理延迟**：由于需要生成多个候选并进行排序和插值，实时性较差，不适合对响应速度要求极高的场景。
*   **反馈闭环不成熟**：目前无法实现真正有效的“生成-验证-修正”迭代循环，主要受限于当前 VLM 对视频细节理解的准确度。
*   **依赖外部数据库**：RAG 阶段的效果高度依赖于预设的关系图谱和修饰符质量。

（完）
