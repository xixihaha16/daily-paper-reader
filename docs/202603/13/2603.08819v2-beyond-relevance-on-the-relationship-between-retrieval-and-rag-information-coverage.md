---
title: "Beyond Relevance: On the Relationship Between Retrieval and RAG Information Coverage"
title_zh: 超越相关性：论检索与 RAG 信息覆盖度之间的关系
authors: "Saron Samuel, Alexander Martin, Eugene Yang, Andrew Yates, Dawn Lawrie, Ian Soboroff, Laura Dietz, Benjamin Van Durme"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08819v2"
tags: ["query:rag"]
score: 9.0
evidence: 检索与RAG信息覆盖率关系的系统研究
tldr: 本研究系统探讨了检索质量与检索增强生成（RAG）系统生成效果之间的关系。通过在TREC NeuCLIR、TREC RAG和WikiVideo等文本及多模态基准上分析多种检索技术栈和RAG流水线，研究发现基于覆盖率的检索指标与生成响应中的信息点覆盖率之间存在显著的强相关性。这一发现证明了检索指标可以作为评估RAG最终生成性能的可靠早期代理指标，为优化RAG系统提供了实证依据。
selection_source: fresh_fetch
motivation: 旨在探究上游检索指标是否能作为RAG系统最终生成响应信息覆盖率的可靠早期预测指标。
method: 在多个文本和多模态基准上，通过分析15种文本和10种多模态检索栈以及多种RAG流水线，系统评估检索与生成指标的相关性。
result: 实验证明基于覆盖率的检索指标与生成响应中的信息点覆盖率在主题和系统层面均具有强相关性。
conclusion: 研究结果支持将检索指标作为RAG性能的有效代理，并指出检索与生成目标的对齐程度是影响这种相关性的关键因素。
---

## 摘要
检索增强生成（RAG）系统将文档检索与生成模型相结合，以处理报告生成等复杂的信息寻求任务。虽然检索质量与生成效果之间的关系看似直观，但尚未得到系统的研究。我们研究了上游检索指标是否可以作为最终生成响应的信息覆盖度的可靠早期指标。通过在两个文本 RAG 基准测试（TREC NeuCLIR 2024 和 TREC RAG 2024）以及一个多模态基准测试（WikiVideo）上进行实验，我们分析了跨四个 RAG 流水线和多个评估框架（Auto-ARGUE 和 MiRAGE）的 15 个文本检索栈和 10 个多模态检索栈。我们的研究结果表明，在主题和系统层面，基于覆盖度的检索指标与生成响应中的关键信息点（nugget）覆盖度之间存在强相关性。当检索目标与生成目标一致时，这种关系最为显著，尽管更复杂的迭代 RAG 流水线可以部分地将生成质量与检索效能解耦。这些发现为使用检索指标作为 RAG 性能的代理指标提供了经验支持。

## Abstract
Retrieval-augmented generation (RAG) systems combine document retrieval with a generative model to address complex information seeking tasks like report generation. While the relationship between retrieval quality and generation effectiveness seems intuitive, it has not been systematically studied. We investigate whether upstream retrieval metrics can serve as reliable early indicators of the final generated response's information coverage. Through experiments across two text RAG benchmarks (TREC NeuCLIR 2024 and TREC RAG 2024) and one multimodal benchmark (WikiVideo), we analyze 15 text retrieval stacks and 10 multimodal retrieval stacks across four RAG pipelines and multiple evaluation frameworks (Auto-ARGUE and MiRAGE). Our findings demonstrate strong correlations between coverage-based retrieval metrics and nugget coverage in generated responses at both topic and system levels. This relationship holds most strongly when retrieval objectives align with generation goals, though more complex iterative RAG pipelines can partially decouple generation quality from retrieval effectiveness. These findings provide empirical support for using retrieval metrics as proxies for RAG performance.

---

## 论文详细总结（自动生成）

### 论文总结：超越相关性——论检索与 RAG 信息覆盖度之间的关系

#### 1. 核心问题与整体含义（研究动机和背景）
随着信息系统从简单的文档检索转向生成式报告（Report Generation），RAG（检索增强生成）已成为主流架构。然而，学术界长期存在一个假设：**上游检索质量越好，下游生成效果就越好**。尽管这在直觉上成立，但缺乏系统性的实证研究。
本论文的核心问题是：**上游检索指标（尤其是关注信息覆盖度的指标）是否可以作为 RAG 最终生成响应质量的可靠早期预测指标（Proxy）？** 研究动机在于，端到端的 RAG 评估成本极高（涉及 LLM 调用和复杂的人工/模型评测），如果能证明检索指标与生成质量强相关，开发者便可以在开发早期通过优化检索指标来间接提升 RAG 性能，从而降低实验成本和噪声。

#### 2. 论文提出的方法论
论文并未提出新的 RAG 算法，而是提出了一套**系统性的相关性分析框架**，旨在量化检索与生成之间的纽带：
*   **核心思想**：将评估重点从传统的“文档相关性”转向“信息覆盖度（Information Coverage）”。在报告生成任务中，冗余的文档无益，系统需要的是涵盖多个不同切面（Facets）的信息。
*   **关键技术细节**：
    *   **信息点（Nugget）评估**：使用“金块（Nuggets）”作为原子信息单位。通过 Auto-ARGUE（将 Nugget 转化为问答对）和 MiRAGE（将 Nugget 转化为声明）框架，衡量生成文本对预定义信息点的覆盖率。
    *   **检索指标选择**：对比了 $\alpha$-nDCG（考虑多样性和冗余惩罚）、nDCG（基于 Nugget 标签）和 Subtopic Recall（子主题召回率）。
    *   **相关性量化**：使用 **Pearson 相关系数**，分别在“主题级别（Topic-level）”和“系统级别（System-level）”分析检索得分与生成得分的一致性。

#### 3. 实验设计
研究涵盖了文本和多模态两个领域，设计非常全面：
*   **数据集/场景**：
    1.  **TREC NeuCLIR 2024**：多语言报告生成任务（中、俄、波斯语）。
    2.  **TREC RAG 2024**：基于 MS MARCO 的复杂问答任务。
    3.  **WikiVideo**：多模态任务，根据多个视频编写百科式文章。
*   **检索技术栈（Retrieval Stacks）**：
    *   文本：包含 BM25、PLAID-X（后期交互）、LSR（学习型稀疏检索）、Qwen3-8B Embed（密集向量）以及 RRF 融合，配合 Qwen3-8B 和 Rank1-7B 重排序器，共 15 种组合。
    *   多模态：包含 CLIP、LanguageBind、Video-ColBERT 等 10 种组合。
*   **RAG 流水线（Pipelines）**：
    *   线性流水线：GPT-Researcher（单查询/多查询）、Bullet List（提取式）。
    *   迭代流水线：LangGraph（具备自我反思和多轮检索能力）。

#### 4. 资源与算力
*   **模型使用**：文本 RAG 统一使用 **Llama-70B-Instruct** 作为骨干 LLM；多模态 RAG 使用 **Qwen2.5-VL-72B**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。但作者提到迭代式 RAG（如 LangGraph）计算成本极高，因此仅在部分数据集上进行了测试。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   文本部分：15 种检索栈 × 4 种 RAG 流水线 × 2 个数据集（共 19+55 个主题）。
    *   多模态部分：10 种检索栈 × 1 种生成系统 × 57 个主题。
*   **充分性评价**：实验设计**非常充分且客观**。作者不仅考虑了同领域的内部相关性，还进行了跨数据集、跨任务目标（如用相关性指标预测覆盖率效果）的交叉验证。引入不同复杂度的 RAG 流水线（从简单到迭代）有效地揭示了系统复杂度对相关性的影响。

#### 6. 主要结论与发现
1.  **强相关性确认**：基于 Nugget 的检索指标（尤其是 **$\alpha$-nDCG**）与生成的 Nugget 覆盖率在主题和系统层面均表现出强相关性。
2.  **指标对齐至关重要**：当检索评估的目标（覆盖度）与生成评估的目标一致时，相关性最强。传统的“文档相关性”指标在复杂报告任务中预测力较弱。
3.  **流水线复杂度的解耦效应**：简单的线性 RAG 流水线高度依赖检索质量；而**复杂的迭代式流水线（如 LangGraph）会部分“解耦”这种关系**——LLM 可以通过多轮检索弥补初始检索的不足，但这也增加了系统对 LLM 适应检索模型能力的依赖。
4.  **多模态发现**：在视频 RAG 中，检索质量与生成的**事实准确性（Precision）**相关性更高，而非覆盖率，这可能与多模态模型过度依赖参数化知识有关。

#### 7. 优点
*   **实证支持**：为“检索领先指标”提供了坚实的实证基础，对 RAG 系统优化具有直接指导意义。
*   **跨模态验证**：研究不仅限于文本，还扩展到了视频 RAG，证明了结论的普适性。
*   **前瞻性**：使用了最新的 TREC 2024 评测数据和最前沿的检索/重排模型（如 Qwen3, Rank1）。

#### 8. 不足与局限
*   **数据标注偏差**：由于 TREC RAG 2024 原始数据缺乏“信息点-文档”的映射，作者使用 Llama-70B 自动补全了这些标签，虽然经过验证，但仍可能引入模型偏见。
*   **多模态实验单一**：受限于计算成本，多模态实验仅使用了一种生成流水线，可能无法完全代表所有多模态 RAG 的行为。
*   **评估维度局限**：研究主要关注“信息覆盖度”，未深入探讨检索质量与生成文本的“流畅度”、“逻辑性”或“幻觉率”之间的关系。

（完）
