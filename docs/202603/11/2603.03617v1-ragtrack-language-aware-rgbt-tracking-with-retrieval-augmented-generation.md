---
title: "RAGTrack: Language-aware RGBT Tracking with Retrieval-Augmented Generation"
title_zh: RAGTrack：基于检索增强生成的语言感知 RGBT 追踪
authors: "Hao Li, Yuhao Wang, Wenning Hao, Pingping Zhang, Dong Wang, Huchuan Lu"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03617v1"
tags: ["query:rag"]
score: 9.0
evidence: 结合检索增强生成的 RGBT 追踪
tldr: 本研究针对RGBT跟踪中缺乏语言引导及模态差异导致的鲁棒性不足问题，提出了RAGTrack框架。该方法首先利用多模态大模型为基准数据集自动生成文本标注；随后设计了多模态Transformer编码器进行统一建模，并通过自适应Token融合减少冗余。核心贡献是引入检索增强生成（RAG）机制进行时序语言推理，动态维护知识库以应对目标外观变化，在多个基准上实现了领先性能。
selection_source: fresh_fetch
motivation: 现有RGBT跟踪器仅依赖初始帧视觉信息，缺乏语言引导且受限于模态差异和搜索区域冗余。
method: 提出RAGTrack框架，结合MLLM自动标注、多模态编码器、自适应Token融合及基于RAG的上下文推理模块。
result: 在四个主流RGBT跟踪基准测试中均取得了最先进的性能表现。
conclusion: 通过引入语言感知和检索增强生成机制，显著增强了RGBT跟踪在复杂环境下的鲁棒性和适应性。
---

## 摘要
RGB-红外（RGBT）追踪旨在通过融合可见光和热红外模态，在各种环境条件下实现鲁棒的目标定位。然而，现有的 RGBT 追踪器仅依赖初始帧的视觉信息进行目标建模，由于缺乏语言引导，无法适应外观变化。此外，当前的方法受限于冗余的搜索区域和异构模态差异，导致背景干扰。为了解决这些问题，我们首先将文本描述引入 RGBT 追踪基准。这是通过一个利用多模态大语言模型（MLLMs）自动生成文本标注的流水线实现的。随后，我们提出了 RAGTrack，一种用于鲁棒 RGBT 追踪的新型检索增强生成框架。为此，我们引入了一个多模态 Transformer 编码器（MTE）用于统一的视觉-语言建模。然后，我们设计了一个自适应令牌融合（ATF）模块，用于选择与目标相关的令牌，并基于跨模态相关性进行通道交换，从而减轻搜索冗余和模态差异。最后，我们提出了一个上下文感知推理模块（CRM）来维护动态知识库，并采用检索增强生成（RAG）来实现时序语言推理，以进行鲁棒的目标建模。在四个 RGBT 基准上的广泛实验表明，我们的框架在各种挑战性场景下均达到了最先进的性能。源代码可在 https://github.com/IdolLab/RAGTrack 获取。

## Abstract
RGB-Thermal (RGBT) tracking aims to achieve robust object localization across diverse environmental conditions by fusing visible and thermal infrared modalities. However, existing RGBT trackers rely solely on initial-frame visual information for target modeling, failing to adapt to appearance variations due to the absence of language guidance. Furthermore, current methods suffer from redundant search regions and heterogeneous modality gaps, causing background distraction. To address these issues, we first introduce textual descriptions into RGBT tracking benchmarks. This is accomplished through a pipeline that leverages Multi-modal Large Language Models (MLLMs) to automatically produce texual annotations. Afterwards, we propose RAGTrack, a novel Retrieval-Augmented Generation framework for robust RGBT tracking. To this end, we introduce a Multi-modal Transformer Encoder (MTE) for unified visual-language modeling. Then, we design an Adaptive Token Fusion (ATF) to select target-relevant tokens and perform channel exchanges based on cross-modal correlations, mitigating search redundancies and modality gaps. Finally, we propose a Context-aware Reasoning Module (CRM) to maintain a dynamic knowledge base and employ a Retrieval-Augmented Generation (RAG) to enable temporal linguistic reasoning for robust target modeling. Extensive experiments on four RGBT benchmarks demonstrate that our framework achieves state-of-the-art performance across various challenging scenarios. The source code is available https://github.com/IdolLab/RAGTrack.

---

## 论文详细总结（自动生成）

这是一份关于论文《RAGTrack: Language-aware RGBT Tracking with Retrieval-Augmented Generation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 RGB-红外（RGBT）目标追踪方法主要依赖初始帧的视觉信息进行建模，在面对目标外观剧烈变化、环境光照极差或存在相似背景干扰时，容易发生漂移。
*   **研究动机**：
    *   **语义缺失**：纯视觉模板难以捕捉目标的抽象语义（如类别、属性、运动状态），导致目标与背景区分度不足。
    *   **模态鸿沟与冗余**：RGB 和热红外模态之间存在异构差异，且搜索区域包含大量冗余背景信息。
    *   **缺乏时序推理**：现有方法难以利用历史帧的上下文信息进行动态推理。
*   **整体含义**：本文首次将**文本描述**引入 RGBT 追踪，并提出 **RAGTrack** 框架，利用**检索增强生成（RAG）**技术结合多模态大模型（MLLM），实现具备语言感知能力的时序鲁棒追踪。

### 2. 方法论
RAGTrack 框架由三个核心组件构成：
*   **多模态 Transformer 编码器 (MTE)**：
    *   将模板图像、搜索图像和文本描述（由 MLLM 预先生成）统一转化为 Token 序列。
    *   引入“推理 Token”（Reasoning Token）和序列前缀，在统一的 Transformer 架构中进行视觉-语言融合建模。
*   **自适应 Token 融合 (ATF)**：
    *   **动态 Token 选择**：利用自注意力分数识别并保留与目标最相关的 Token（保留率 $\gamma$），剔除背景噪声。
    *   **自适应通道交换**：基于跨模态相关性，在 RGB 和红外特征通道间进行动态交换，缩小模态鸿沟。
*   **上下文感知推理模块 (CRM)**：
    *   **RAG 机制**：构建动态知识库存储历史文本特征。
    *   **检索与增强**：在追踪过程中检索最相关的历史上下文，通过交叉注意力增强当前的推理 Token。
    *   **动态生成**：利用 MLLM（如 Qwen2.5-VL）根据追踪结果实时生成/更新目标描述，应对外观演变。

### 3. 实验设计
*   **数据集与场景**：在四个主流 RGBT 追踪基准上进行评估：**GTOT**、**RGBT210**、**RGBT234** 和 **LasHeR**。涵盖了遮挡、光照变化、高速运动等多种挑战属性。
*   **Benchmark 与对比方法**：
    *   对比了超过 20 种最先进（SOTA）的追踪器，包括 ViPT (2023)、TBSI (2023)、SDSTrack (2024)、AINet (2025)、STTrack (2025) 等。
    *   **数据标注**：利用 MLLM 自动生成流水线为 LasHeR 等数据集补全了超过 51 万条文本描述标注。
*   **评价指标**：精确率（PR/MPR）、成功率（SR/MSR）及归一化精确率（NPR）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA V100 GPU**。
*   **训练细节**：Batch size 为 16，采用 AdamW 优化器，学习率为 $10^{-4}$。
*   **模型配置**：视觉骨干网络采用 HiViT-B，文本编码器采用 CLIP，在线推理使用的 MLLM 为 Qwen2.5-VL-3B。
*   **运行效率**：追踪速度达到 **24.3 FPS**（在 V100 上测试），计算量为 62.7G FLOPs。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个大型数据集上进行了全量对比，实验结果覆盖了 19 种细粒度属性分析。
*   **消融实验**：非常详尽，包括：
    1.  各组件（MTE, ATF, CRM）的有效性分析。
    2.  不同融合位置（层数）的影响。
    3.  不同融合范式（对比 TBSI, BSI 等）的优劣。
    4.  超参数（Token 保留率、通道交换率、知识库大小、检索数量）的敏感度分析。
    5.  **鲁棒性测试**：专门测试了在“文本缺失”情况下的性能，证明了即使没有文本输入，模型依然优于现有纯视觉方法。
*   **客观性**：通过多数据集对比和详尽的参数讨论，实验设计科学、客观，对比基准涵盖了最新的顶会论文。

### 6. 主要结论与发现
*   **性能领先**：RAGTrack 在所有四个基准上均刷新了 SOTA 记录。例如在 LasHeR 上 PR 达到 76.8%，SR 达到 61.1%，显著超过之前的最优方法。
*   **语言的价值**：引入文本描述能显著提升模型在“全遮挡”和“目标出视野”场景下的找回能力。
*   **RAG 的有效性**：动态维护知识库并进行时序推理，比单纯依赖初始帧模板更具鲁棒性。
*   **效率平衡**：尽管引入了复杂的 RAG 和 MLLM，通过合理的 Token 选择和轻量化设计，模型仍能保持近乎实时的运行速度。

### 7. 优点（亮点）
*   **首创性**：首次将 RAG 框架和 MLLM 引入 RGBT 追踪领域，填补了该领域语言感知能力的空白。
*   **自动化标注**：提出了一套利用 MLLM 自动生成高质量追踪描述的流水线，解决了 RGBT 数据集缺乏文本标注的难题。
*   **动态演进**：CRM 模块使追踪器具备了“记忆”和“动态更新语义”的能力，而非死板地匹配初始模板。
*   **即插即用且鲁棒**：ATF 模块在不显著增加参数量的情况下有效解决了模态融合问题，且模型对文本缺失具有很强的容错性。

### 8. 不足与局限
*   **推理延迟风险**：虽然文中提到 24.3 FPS，但在实际应用中，频繁调用 MLLM（如 Qwen-VL）进行描述生成可能会带来额外的计算开销或延迟，尤其是在嵌入式设备上。
*   **幻觉风险**：尽管有精炼步骤，但 MLLM 自动生成的描述仍可能存在“幻觉”，在极端复杂的场景下，错误的文本引导可能导致追踪失败。
*   **应用限制**：目前主要针对单目标追踪，在多目标或超长视频序列中的知识库管理效率（如内存爆炸问题）尚待进一步验证。

（完）
