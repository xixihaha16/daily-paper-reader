---
title: Retrieval-Augmented Generation with Covariate Time Series
title_zh: 协变量时间序列的检索增强生成
authors: "Kenny Ye Liang, Zhongyi Pei, Huan Zhang, Yuhui Liu, Shaoxu Song, Jianmin Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04951v1"
tags: ["query:rag"]
score: 9.0
evidence: 用于时间序列的免训练 RAG 框架
tldr: 针对工业预测性维护中数据稀缺、序列短且协变量耦合的挑战，本文提出RAG4CTS框架。该框架是首个面向协变量时间序列的免训练RAG方案，通过构建原生知识库、双阶段双加权检索机制及智能体驱动的上下文优化，实现了对历史工况的精准对齐。在南航PRSOV故障预测中的应用证明，该方法显著提升了预测精度并成功识别真实故障。
selection_source: fresh_fetch
motivation: 现有的时间序列RAG方法在处理数据稀缺、短瞬态及协变量耦合的工业场景时，难以有效区分相似工况。
method: 提出了RAG4CTS框架，包含分层原生知识库、双阶段双加权检索机制以及自监督的智能体驱动上下文增强策略。
result: 实验表明RAG4CTS在PRSOV预测精度上显著优于现有基准，并在南航实际部署中成功识别出故障且保持零误报。
conclusion: RAG4CTS为复杂工业时间序列预测提供了一种高效、免训练且具备物理感知能力的检索增强生成新范式。
---

## 摘要
尽管检索增强生成（RAG）极大地增强了大语言模型（LLMs），但将这一范式扩展到时间序列基础模型（TSFMs）仍然是一个挑战。这一点在调压切断阀（PRSOV）的预测性维护中得到了体现，这是一个高风险的工业场景，具有（1）数据稀缺、（2）短暂态序列以及（3）协变量耦合动力学的特征。不幸的是，现有的时间序列 RAG 方法主要依赖于生成的静态向量嵌入和可学习的上下文增强器，在这些稀缺、暂态且协变量耦合的场景中，可能无法区分相似的状态（regimes）。为了解决这些局限性，我们提出了 RAG4CTS，这是一个针对协变量时间序列的、状态感知的、无需训练的 RAG 框架。具体而言，我们构建了一个分层的时间序列原生知识库，以实现原始历史状态的无损存储和物理启发式检索。我们设计了一种两阶段双加权检索机制，通过逐点和多变量相似性来对齐历史趋势。对于上下文增强，我们引入了一种智能体驱动的策略，以自监督的方式动态优化上下文。在 PRSOV 上的大量实验表明，我们的框架在预测准确性方面显著优于最先进的基准模型。所提出的系统已部署在中国南方航空公司的 Apache IoTDB 中。自部署以来，我们的方法在两个月内成功识别了一次 PRSOV 故障，且零误报。

## Abstract
While RAG has greatly enhanced LLMs, extending this paradigm to Time-Series Foundation Models (TSFMs) remains a challenge. This is exemplified in the Predictive Maintenance of the Pressure Regulating and Shut-Off Valve (PRSOV), a high-stakes industrial scenario characterized by (1) data scarcity, (2) short transient sequences, and (3) covariate coupled dynamics. Unfortunately, existing time-series RAG approaches predominantly rely on generated static vector embeddings and learnable context augmenters, which may fail to distinguish similar regimes in such scarce, transient, and covariate coupled scenarios. To address these limitations, we propose RAG4CTS, a regime-aware, training-free RAG framework for Covariate Time-Series. Specifically, we construct a hierarchal time-series native knowledge base to enable lossless storage and physics-informed retrieval of raw historical regimes. We design a two-stage bi-weighted retrieval mechanism that aligns historical trends through point-wise and multivariate similarities. For context augmentation, we introduce an agent-driven strategy to dynamically optimize context in a self-supervised manner. Extensive experiments on PRSOV demonstrate that our framework significantly outperforms state-of-the-art baselines in prediction accuracy. The proposed system is deployed in Apache IoTDB within China Southern Airlines. Since deployment, our method has successfully identified one PRSOV fault in two months with zero false alarm.

---

## 论文详细总结（自动生成）

这是一份关于论文《Retrieval-Augmented Generation with Covariate Time Series》（协变量时间序列的检索增强生成）的结构化深入总结：

### 1. 核心问题与研究动机
论文聚焦于**工业高风险场景下时间序列预测**的挑战，特别是以飞机“调压切断阀（PRSOV）”的预测性维护为典型案例。
*   **研究背景**：虽然时间序列基础模型（TSFM）在通用领域表现出色，但在工业应用中面临三大难题：
    1.  **数据稀缺**：关键故障或工况极少发生，难以支撑深度学习模型的训练。
    2.  **短瞬态序列**：状态切换极快（如10秒内仅18个数据点），信息密度极低。
    3.  **协变量耦合**：目标变量（如压力）受外部控制变量（如发动机转速、上游压力）的被动驱动，忽略这些物理逻辑会导致预测失真。
*   **现有方案不足**：现有的时间序列RAG方法（如TS-RAG）依赖于静态向量嵌入（会丢失短序列的数值精度）和可学习的适配器（在数据稀缺时难以收敛），且往往忽略了协变量的物理对齐。

### 2. 方法论（RAG4CTS）
论文提出了 **RAG4CTS**，一个状态感知、免训练的协变量时间序列RAG框架。其核心组件包括：
*   **分层原生知识库（Hierarchical KB）**：采用树状结构（机型-机号-设备-传感器）直接存储原始物理序列，避免了向量化带来的精度损失，确保短瞬态信号的完整性。
*   **两阶段双加权检索机制**：
    *   **双加权（Bi-weighted）**：结合“关键点权重”（利用未来已知的控制变量，对近期历史和未来控制输入加权）和“协变量权重”（利用互信息 MI 衡量各变量对目标的物理影响程度）。
    *   **两阶段过滤**：第一阶段通过加权余弦相似度进行“形状对齐”；第二阶段通过加权矩阵轮廓（Matrix Profile）进行“数值精度对齐”，确保检索到的参考案例在物理逻辑上与当前工况一致。
*   **智能体驱动的上下文增强（Agent-driven Augmentation）**：
    *   将检索到的 Top-1 样本作为“智能体（Agent）”，利用其已知的真实值作为验证集。
    *   通过自监督的贪心搜索，动态确定最适合当前查询的上下文拼接数量 $k^*$，从而在增强信息量与减少注意力噪声之间取得平衡。

### 3. 实验设计
*   **数据集**：中国南方航空（CSA）PRSOV 真实数据集，涵盖 B777 和 A320 两种机型，分为左右发动机四个子集。
*   **任务设置**：短时瞬态预测（输入6个点，预测未来12个点）。
*   **对比基准（Benchmarks）**：
    *   **深度学习模型**：DLinear, PatchTST, iTransformer, TimeMixer, TimeXer, Pyraformer。
    *   **零样本 TSFM**：Chronos-2, TimesFM, Sundial, Chronos-Bolt。
    *   **RAG 方案**：TS-RAG（基于向量嵌入和训练适配器的方案）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **部署环境**：该系统已实际部署在 Apache IoTDB 的 AINode 模块中，强调了其在数据库内直接进行推理的能力，减少了数据迁移开销。由于核心框架是“免训练（Training-free）”的，其主要算力消耗在于检索和 TSFM 的推理过程。

### 5. 实验数量与充分性
*   **实验规模**：论文在四个不同的工业子数据集上进行了测试，并进行了详尽的消融实验。
*   **消融实验涵盖**：知识库范围的影响、检索指标的敏感性（余弦 vs DTW vs 矩阵轮廓）、加权方案的有效性、以及上下文长度 $k$ 的动态选择。
*   **充分性评价**：实验设计非常充分且具有高度的工业实战性。通过对比“零样本”、“微调”以及“RAG”等多种模式，客观证明了在极度缺乏数据的情况下，检索增强比模型微调更有效。

### 6. 主要结论与发现
*   **性能领先**：RAG4CTS 在所有测试集上的 MSE 和 MAE 均显著低于现有最先进模型（SOTA）。
*   **物理一致性**：引入协变量权重和未来控制逻辑后，模型能够准确捕捉物理系统的响应，而非仅仅进行数值拟合。
*   **工业价值**：在南航部署的两个月内，系统成功识别出一次真实的 PRSOV 故障，且实现了**零误报**，证明了其在实际生产环境中的可靠性。

### 7. 优点与亮点
*   **免训练特性**：无需针对特定设备进行昂贵的模型重训，非常适合长尾、稀缺的工业场景。
*   **原生存储与检索**：放弃了通用的向量数据库，改用时间序列原生存储，解决了短序列在向量化过程中的“噪声淹没”问题。
*   **自适应增强**：通过 Agent 机制动态调整上下文，解决了 RAG 中常见的“检索越多噪声越大”的痛点。

### 8. 不足与局限
*   **知识库依赖**：作为 RAG 框架，其性能高度依赖于知识库中历史工况的丰富程度。如果遇到全新的、从未出现过的物理失效模式，检索可能失效。
*   **跨机型迁移限制**：实验显示，跨机型（如 B777 数据用于 A320）的检索效果有限，说明物理系统的差异性限制了知识库的通用边界。
*   **实时性挑战**：虽然是免训练，但两阶段检索（尤其是矩阵轮廓计算）在超大规模知识库下的检索延迟可能成为实时预测的瓶颈（尽管文中未提及延迟数据）。

（完）
