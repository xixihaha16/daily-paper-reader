---
title: Type-Aware Retrieval-Augmented Generation with Dependency Closure for Solver-Executable Industrial Optimization Modeling
title_zh: 面向求解器可执行工业优化建模的具有依赖闭包的类型感知检索增强生成
authors: "Y. Zhong, R. Huang, M. Wang, Z. Guo, YC. Li, M. Yu, Z. Jin"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03180v1"
tags: ["query:rag"]
score: 9.0
evidence: 用于工业优化建模的类型感知RAG
tldr: 针对工业优化建模中大模型生成代码易出现类型不一致和依赖缺失导致无法执行的问题，本文提出一种类型感知的检索增强生成（RAG）方法。该方法通过构建领域特定的类型化知识库和知识图谱，利用依赖闭合机制确保生成代码包含所有必需的符号声明。在电池生产调度和车间调度案例中，该方法成功生成了可执行且最优的求解模型，显著优于传统RAG基准。
selection_source: fresh_fetch
motivation: 现有大模型在自动化工业优化建模时，常因缺乏完整的依赖上下文和类型约束而生成无法编译运行的代码。
method: 提出一种结合类型感知与依赖闭合的RAG方法，通过解析异构数据构建知识图谱，并利用依赖传播计算最小闭合上下文。
result: 在需求响应优化和柔性作业车间调度两个复杂案例中，该方法均能生成可执行模型并达到最优解，而传统基准方法均告失败。
conclusion: 强制执行类型感知的依赖闭合是消除结构性幻觉、确保大模型在复杂工程优化任务中生成可执行代码的关键。
---

## 摘要
自动化工业优化建模需要将自然语言需求可靠地转化为求解器可执行的代码。然而，由于缺少声明、类型不一致以及依赖上下文不完整，大语言模型经常生成不可编译的模型。我们提出了一种类型感知的检索增强生成（RAG）方法，该方法通过强制建模实体类型和最小依赖闭包来确保可执行性。与索引非结构化文本的现有 RAG 方法不同，我们的方法通过将学术论文和求解器代码等异构源解析为类型化单元，并将其数学依赖关系编码在知识图中，构建了一个特定领域的类型化知识库。给定自然语言指令，该方法执行混合检索，并通过图上的依赖传播计算最小依赖闭包上下文，即求解器可执行代码所需的最小类型化符号集。我们在两个约束密集型工业案例中验证了该方法：电池生产中的需求响应优化和柔性作业车间调度。在第一个案例中，我们的方法生成了一个包含需求响应激励和负荷削减约束的可执行模型，在保持盈利能力的同时实现了削峰；传统的 RAG 基准模型则失败了。在第二个案例中，它持续生成能够达到已知最优解的可编译模型，展示了鲁棒的跨领域泛化能力；基准模型完全失败。消融实验证实，强制执行类型感知依赖闭包对于避免结构性幻觉和确保可执行性至关重要，解决了在复杂工程优化任务中部署大语言模型的一个关键障碍。

## Abstract
Automated industrial optimization modeling requires reliable translation of natural-language requirements into solver-executable code. However, large language models often generate non-compilable models due to missing declarations, type inconsistencies, and incomplete dependency contexts. We propose a type-aware retrieval-augmented generation (RAG) method that enforces modeling entity types and minimal dependency closure to ensure executability. Unlike existing RAG approaches that index unstructured text, our method constructs a domain-specific typed knowledge base by parsing heterogeneous sources, such as academic papers and solver code, into typed units and encoding their mathematical dependencies in a knowledge graph. Given a natural-language instruction, it performs hybrid retrieval and computes a minimal dependency-closed context, the smallest set of typed symbols required for solver-executable code, via dependency propagation over the graph. We validate the method on two constraint-intensive industrial cases: demand response optimization in battery production and flexible job shop scheduling. In the first case, our method generates an executable model incorporating demand-response incentives and load-reduction constraints, achieving peak shaving while preserving profitability; conventional RAG baselines fail. In the second case, it consistently produces compilable models that reach known optimal solutions, demonstrating robust cross-domain generalization; baselines fail entirely. Ablation studies confirm that enforcing type-aware dependency closure is essential for avoiding structural hallucinations and ensuring executability, addressing a critical barrier to deploying large language models in complex engineering optimization tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Type-Aware Retrieval-Augmented Generation with Dependency Closure for Solver-Executable Industrial Optimization Modeling》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在工业领域，将自然语言需求转化为可被求解器（如 LINGO, Gurobi）执行的数学优化模型极具挑战。大语言模型（LLM）在生成此类代码时，常因缺乏领域约束而产生“结构性幻觉”，表现为变量未声明、类型不一致（如混淆参数与变量）以及依赖上下文不完整，导致生成的模型无法编译或求解。
*   **核心问题**：如何确保 LLM 生成的工业优化模型在符号上是完备的、在类型上是准确的，并且能够直接在求解器中运行。

### 2. 方法论
论文提出了一种结合**类型感知（Type-Aware）**与**依赖闭包（Dependency Closure）**的检索增强生成（RAG）框架，核心流程如下：
*   **异构知识图谱构建**：
    *   解析学术论文（提取数学符号语义）和求解器代码（提取可执行语法结构）。
    *   定义类型化实体模式：包括决策变量、参数、索引集、约束、目标函数和辅助规则。
    *   建立依赖关系边：如 `used_in`（变量用于约束）和 `depends_on`（约束依赖于参数）。
*   **混合检索机制**：
    *   **语义检索**：利用向量数据库（BGE-M3 嵌入）获取概念背景。
    *   **结构化检索**：在知识图谱中根据查询意图定位种子节点。
*   **最小依赖闭包计算**：
    *   这是确保可执行性的关键。给定一个目标实体 $e$，系统通过广度优先搜索（BFS）遍历图中的依赖边，计算出使 $e$ 定义完备所需的最小符号集合 $C(e)$。
    *   该闭包确保了生成的代码片段包含所有必需的变量声明和参数定义，消除了“引用未定义符号”的错误。

### 3. 实验设计
*   **实验场景**：
    1.  **锂电池模组生产需求响应（DR）优化**：涉及复杂的机器饥饿、缓冲区阻塞约束及动态激励目标函数。
    2.  **柔性作业车间调度问题（FJSP）**：测试跨领域泛化能力，涉及机器分配、工序排序等 NP-hard 问题。
*   **Benchmark 与对比方法**：
    *   使用 **Behnke 标杆实例**（针对 FJSP）。
    *   **对比基准**：传统 RAG（非类型感知）、仅使用学术论文检索、仅使用代码检索、以及无 RAG 的 LLM 直接生成。
*   **评估指标**：模型可编译性（Compilability）、可求解性（Solvability）、解的最优性（Optimization Quality）。

### 4. 资源与算力
*   **硬件配置**：Intel Core i9-14900K (24核), 128 GB 内存。
*   **软件/模型**：
    *   **LLM**：WizardCoder-33B-v1.1。
    *   **嵌入模型**：BGE-M3。
    *   **数据库**：ChromaDB (向量), Neo4j (图)。
    *   **求解器**：LINGO 18.0, Python 3.9。
*   **算力说明**：文中未明确提及 GPU 的具体型号和训练时长（主要使用现成模型进行推理和 RAG 流程），但给出了求解器运行的时间限制（120秒）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 FJSP 案例中进行了 20 次独立运行以测试稳定性。
    *   针对不同知识源配置进行了消融实验（配置1：仅论文；配置2：仅代码；配置3：异构融合）。
    *   测试了 5 组不同规模的 Behnke 实例（从 10x20 到 50x20）。
*   **充分性评价**：实验设计较为充分，涵盖了从简单约束修改到复杂跨领域建模的场景。通过消融实验客观地证明了“依赖闭包”和“异构数据源”对消除幻觉的必要性。

### 6. 主要结论与发现
*   **可靠性飞跃**：所提方法在 FJSP 任务中达到了 **95% 的成功生成率**，而传统 RAG 基准在所有测试实例中均告失败（0% 成功率），无法生成可执行代码。
*   **最优性保证**：生成的模型不仅可运行，且在规定时间内均能找到已知最优解。
*   **依赖闭包的重要性**：实验证明，仅仅依靠语义相似性检索是不够的，必须通过图结构强制提取符号间的数学依赖链，才能保证代码的逻辑完备性。

### 7. 优点
*   **逻辑严密性**：引入“依赖闭包”概念，将软件工程中的依赖管理思想应用于数学建模，解决了 LLM 生成代码最核心的“符号缺失”问题。
*   **异构对齐**：成功桥接了学术论文的“高层意图”与求解器代码的“底层实现”，实现了跨模态的知识对齐。
*   **实用性强**：直接面向工业求解器，生成的 LINGO 或 Python 代码具有高度的工程落地价值。

### 8. 不足与局限
*   **逻辑索引错误**：尽管解决了符号声明问题，但在处理极其复杂的循环边界（如 off-by-one errors）时，LLM 仍有 5% 的概率出错，说明纯检索无法完全替代逻辑推理。
*   **规模限制**：目前验证的实例属于中等规模，对于超大规模、跨多日的工业调度问题的处理能力尚待验证。
*   **领域覆盖**：虽然测试了两个领域，但对于非线性规划（NLP）或多目标优化等更复杂的数学结构，其类型模式（Schema）可能需要进一步扩展。

（完）
