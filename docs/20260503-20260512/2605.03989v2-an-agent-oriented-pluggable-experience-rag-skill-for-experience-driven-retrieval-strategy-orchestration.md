---
title: An Agent-Oriented Pluggable Experience-RAG Skill for Experience-Driven Retrieval Strategy Orchestration
title_zh: 一种面向智能体的可插拔 Experience-RAG 技能，用于经验驱动的检索策略编排
authors: "Dutao Zhang, Tian Liao"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03989v2"
tags: ["query:rag"]
score: 8.0
evidence: 面向智能体的经验驱动检索策略编排
tldr: 针对传统RAG系统在处理异构任务时检索策略单一的问题，本文提出了一种面向智能体的可插拔“经验RAG技能”（Experience-RAG Skill）。该技能作为智能体与检索器池之间的编排层，能根据当前场景和经验记忆动态选择最优检索策略。在BeIR等多个数据集上的实验表明，该方法显著优于固定检索基准，证明了将检索策略选择封装为可重用技能的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03989v2/fig-001.webp\", \"caption\": \"Figure 1: Overall framework of Experience-RAG Skill. The skill is positioned between the agent and the retriever pool. It transforms the incoming query, history, and metadata into structured scene features, consults reusable experience records, selects a retrieval strategy, invokes the candidate retriever pool, and returns a packaged evidence object to the agent.\", \"page\": 2, \"index\": 1, \"width\": 943, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2605.03989v2/fig-002.webp\", \"caption\": \"Table 2: Comparison with modern baselines.\", \"page\": 3, \"index\": 2, \"width\": 623, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.03989v2/fig-003.webp\", \"caption\": \"Table 1: Overall retrieval performance.\", \"page\": 3, \"index\": 3, \"width\": 623, \"height\": 197}]"
motivation: 现有的RAG系统通常采用固定的检索流程，难以适应事实问答、多跳推理和科学验证等不同任务对检索策略的多样化需求。
method: 提出一种可插拔的检索编排层，通过分析任务场景并咨询经验记忆库，动态选择并执行最合适的检索策略。
result: "在BeIR/nq、hotpotqa和scifact数据集上达到了0.8924的nDCG@10评分，优于固定单检索器基准并与自适应路由方法相当。"
conclusion: 检索策略的选择可以被有效地封装为一种可重用的智能体技能，而非硬编码在顶层工作流中，从而提升系统的灵活性和性能。
---

## 摘要
检索增强生成（RAG）系统通常假设单一固定的检索流水线足以应对各种异构任务，然而事实性问答、多跳推理和科学验证表现出不同的检索偏好。我们提出了 Experience-RAG Skill，这是一种面向智能体的可插拔检索编排层，位于智能体与检索器池之间。该技能分析当前场景，咨询经验记忆，选择合适的检索策略，并向智能体返回结构化证据。在固定候选池的情况下，Experience-RAG Skill 在 BeIR/nq、BeIR/hotpotqa 和 BeIR/scifact 上实现了 0.8924 的综合 nDCG@10，优于固定的单一检索器基准，并与 Adaptive-RAG 风格的路由保持竞争力。结果表明，检索策略选择可以有效地封装为可重用的智能体技能，而不是在上位工作流中进行硬编码。

## 速览
**TLDR**：针对RAG系统在不同任务下检索偏好不一的问题，本文提出Experience-RAG Skill，这是一种面向智能体的可插拔检索编排层。该方法通过分析场景并参考经验记忆，动态选择最优检索策略。在BeIR等多个数据集上的实验表明，该技能显著提升了检索效果，证明了将检索策略选择封装为可复用技能的有效性。 \
**Motivation**：传统的RAG系统通常采用固定的检索流程，难以适应事实问答、多跳推理等异构任务对检索策略的不同需求。 \
**Method**：提出一种可插拔的经验驱动检索技能，通过分析当前场景并咨询经验存储器，在检索器池中动态编排最合适的策略。 \
**Result**：在BeIR/nq、hotpotqa和scifact数据集上达到了0.8924的nDCG@10评分，优于固定检索器基准并与自适应路由方法相当。 \
**Conclusion**：检索策略的选择可以被有效地封装为一种可复用的智能体技能，而非硬编码在顶层工作流中。

---

## Abstract
Retrieval-augmented generation systems often assume that one fixed retrieval pipeline is sufficient across heterogeneous tasks, yet factoid question answering, multi-hop reasoning, and scientific verification exhibit different retrieval preferences. We present Experience-RAG Skill, an agent-oriented pluggable retrieval orchestration layer positioned between the agent and the retriever pool. The proposed skill analyzes the current scene, consults an experience memory, selects an appropriate retrieval strategy, and returns structured evidence to the agent. Under a fixed candidate pool, Experience-RAG Skill achieves an overall nDCG@10 of 0.8924 on BeIR/nq, BeIR/hotpotqa, and BeIR/scifact, outperforming fixed single-retriever baselines and remaining competitive with Adaptive-RAG-style routing. The results suggest that retrieval strategy selection can be productively encapsulated as a reusable agent skill rather than being hard-coded in the upper workflow.

---

## 论文详细总结（自动生成）

以下是对论文《An Agent-Oriented Pluggable Experience-RAG Skill for Experience-Driven Retrieval Strategy Orchestration》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的检索增强生成（RAG）系统通常采用固定的检索流水线。然而，不同类型的任务（如事实问答、多跳推理、科学验证）对检索策略有不同的偏好。例如，密集检索（Dense Retrieval）擅长事实问答，而混合检索（Hybrid Retrieval）在多跳和科学证据匹配中更稳定。
*   **核心问题**：如何让智能体（Agent）能够根据当前任务场景，动态且自主地选择最合适的检索策略，而不是将检索逻辑硬编码在工作流中。
*   **整体含义**：本文将检索策略的选择重新定义为一种“可插拔的智能体技能”（Experience-RAG Skill），作为智能体与检索器池之间的中间编排层。

### 2. 方法论
*   **核心思想**：通过一个包含场景分析、经验咨询、策略路由和结果封装的四步流程，实现检索策略的动态编排。
*   **关键技术细节**：
    *   **场景分析（Scene Analyzer）**：将查询（q）、对话历史（h）和任务元数据（m）转化为结构化的场景特征（s），包括任务类型、领域、上下文长度、问题复杂度等。
    *   **经验记忆（Experience Memory）**：存储过往的记录（e），包含场景特征、各策略的得分向量、最优策略及其领先幅度（Margin）。
    *   **策略路由（Strategy Router）**：根据场景特征和经验记忆选择最优策略（r*）。目前最强的配置是**基于规则的路由**（如：直接任务指向密集检索，多跳/科学任务指向混合RRF检索）。
    *   **结果封装（Result Packager）**：执行检索并返回标准化的证据对象，确保智能体接口的统一性。
*   **公式化表达**：$s = Analyze(q, h, m)$；$r^* = Route(s, M)$；$o = RetrieveAndPackage(q, r^*)$。

### 3. 实验设计
*   **数据集**：选取了 BeIR 基准测试中的三个代表性子集：**BeIR/nq**（事实问答）、**BeIR/hotpotqa**（多跳推理）和 **BeIR/scifact**（科学验证）。
*   **实验规模**：每个数据集抽取 120 个查询，并使用采样后的候选语料库。
*   **对比方法（Baselines）**：
    *   **传统基准**：BM25、重写后 BM25、密集检索（Dense）、混合检索（Hybrid RRF）。
    *   **现代/先进基准**：Adaptive-RAG（自适应路由）、HyDE（假设文档嵌入）、LongRAG（长上下文）、RAPTOR（递归摘要树检索）。
*   **评价指标**：Recall@10、MRR@10、nDCG@10。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及训练/推理的具体时长。由于实验是在采样语料库上进行的，且核心路由目前以规则为主，推测其对算力的即时需求相对较低。

### 5. 实验数量与充分性
*   **实验组数**：涵盖了三个异构任务数据集的对比实验、针对现代 RAG 变体的对比实验、消融实验（验证各组件贡献）以及学习型路由与规则路由的对比。
*   **充分性评价**：
    *   **优点**：实验覆盖了 RAG 领域最主流的几种变体，对比维度较为全面。
    *   **局限**：实验基于“采样语料库”而非全量数据，且每个数据集仅 120 个查询，样本量较小，可能存在一定的统计偏差。此外，缺乏端到端的智能体交互评测。

### 6. 主要结论与发现
*   **性能优越**：Experience-RAG Skill 实现了 0.8924 的综合 nDCG@10，优于所有固定的单一检索策略。
*   **编排价值**：该技能在混合工作负载下的表现优于在单一任务上的表现，证明其核心优势在于“异构编排”而非单一检索算法的改进。
*   **路由策略**：在当前经验规模下，**基于规则的路由**优于基于机器学习的路由（分类或回归），说明经验记忆的利用尚有优化空间。
*   **竞争性**：该方法与复杂的 Adaptive-RAG 性能相当，但提供了更清晰的系统边界和可重用性。

### 7. 优点
*   **模块化与可插拔**：将检索策略封装为技能，降低了智能体上层逻辑的复杂度。
*   **经验驱动**：引入经验记忆概念，使系统具备从过往检索表现中学习和优化的潜力。
*   **结构化输出**：统一了检索结果的返回格式，便于智能体后续的自动化处理。

### 8. 不足与局限
*   **数据规模限制**：实验仅在采样的小规模语料库上进行，在大规模生产环境下的鲁棒性待验证。
*   **路由模型尚不成熟**：学习型路由（Learned Routing）目前未能超越人工规则，限制了系统的自动化进化能力。
*   **静态候选池**：目前假设检索器池是固定的，尚未解决动态增加新检索工具（Onboarding）时的冷启动问题。
*   **评估深度**：缺乏对智能体最终生成质量（Generation Quality）的影响分析，仅停留在检索阶段的评估。

（完）
