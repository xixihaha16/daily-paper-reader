---
title: "Superintelligent Retrieval Agent: The Next Frontier of Information Retrieval"
title_zh: 超智能检索智能体：信息检索的新前沿
authors: "Zeyu Yang, Qi Ma, Jason Chen, Anshumali Shrivastava"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06647v1"
tags: ["query:rag"]
score: 9.0
evidence: 用于RAG的超智能检索代理
tldr: 针对现有检索增强智能体在处理复杂查询时存在的多轮迭代、高延迟和低召回率问题，本文提出超智能检索智能体（SIRA）。SIRA通过LLM离线丰富文档词汇并在线预测查询缺失词汇，结合语料库统计信息进行过滤，将多轮探索性搜索压缩为单次加权BM25检索。该方法在BEIR基准测试中表现优异，实现了高效、可解释且无需训练的精准检索。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06647v1/fig-001.webp\", \"caption\": \"Table 1 Overview of the ten BEIR retrieval benchmarks used in our evaluation, spanning diverse reasoning types including fact-checking, argumentation, citation prediction, and standard QA. The suite covers a broad range of domains and corpus sizes (5K to 5.4M documents), rigorously testing generalization. Rel D/Q denotes the average number of relevant documents per query.\", \"page\": 7, \"index\": 1, \"width\": 979, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.06647v1/fig-002.webp\", \"caption\": \"Figure 1 Three retrieval paradigms compared. (a) Dense retrieval encodes queries and documents into a shared embedding space and performs nearest-neighbor search; the process is one-shot but opaque and requires in-domain supervision. (b) Multi-step agent retrieval uses an LLM to iteratively formulate queries, read retrieved passages, and reformulate over N rounds; later queries benefit from accumulated retrieval context. (c) SIRA produces an expert-level retrieval action in a single shot: the LLM generates an expected-response sketch, validates proposed terms against corpus statistics, and compiles a controlled BM25 query with weighted keywords and constraints, all without reading any retrieved passages.\", \"page\": 3, \"index\": 2, \"width\": 960, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2605.06647v1/fig-003.webp\", \"caption\": \"Table 2 Recall@10 and NDCG@10 on ten BEIR datasets. HyDE, CoT, GrepRAG, ShellAgent, and SIRA use Qwen3.635B-A3B-FP8 (frozen, 3B active parameters); Search-R1 (E5) uses its publicly available checkpoint with an E5 retrieval backend. Neural baselines use official checkpoints: SPARTA (BeIR/sparta-msmarco-distilbert-base-v1), SPLADE (naver/splade-cocondenser-ensembledistil), Doc2Query (doc2query/msmarco-t5-base-v1), E5 (intfloat/e5-base-v2). Best per dataset in bold; second best underlined.\", \"page\": 8, \"index\": 3, \"width\": 979, \"height\": 642}, {\"url\": \"assets/figures/arxiv/2605.06647v1/fig-004.webp\", \"caption\": \"Figure 3 SIRA retrieval answer coverage (top-5 and top-10) vs. six RL-trained agentic QA systems on NQ and HotpotQA. Baseline numbers are the best reported results taken directly from each original paper. All baselines are end-to-end QA pipelines reporting generated-answer accuracy; SIRA is a pure retriever with no reader. Answer coverage requires the gold string to be retreived.\", \"page\": 9, \"index\": 4, \"width\": 730, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2605.06647v1/fig-005.webp\", \"caption\": \"Figure 2 SIRA pipeline overview. Corpus-side enrichment (left) is performed once offline; query-side enrichment (right) runs per query. Both stages apply a DF filter to reject uninformative terms; the query-side filter additionally requires DF > 0 to ensure each expansion term exists in the index. The original query qorig bypasses enrichment (dashed) and is combined with the expansion terms in a single weighted BM25 call.\", \"page\": 5, \"index\": 5, \"width\": 939, \"height\": 465}]"
motivation: 解决现有检索智能体因多轮黑盒搜索导致的效率低下、延迟高及召回率不足的问题。
method: 利用LLM进行离线文档增强和在线查询扩展，并结合语料库统计数据优化检索词，执行单次加权BM25检索。
result: 在十项BEIR基准测试和下游问答任务中，SIRA的性能显著优于稠密检索器和最先进的多轮智能体基准。
conclusion: SIRA证明了通过LLM认知与轻量级语料统计引导的单次词法查询，可以超越昂贵的多轮搜索，实现高效且可解释的检索。
---

## 摘要
检索增强型智能体正日益成为大型组织知识库的接口，但大多数智能体仍将检索视为黑盒：它们发出探索性查询，检查返回的片段，并反复重新表述，直到出现有用的证据。这种方法类似于新手搜索不熟悉的数据库，而不是专家利用对术语和可能证据的强大先验知识进行导航，从而导致不必要的检索轮次、增加的延迟和较差的召回率。我们引入了“超智能检索智能体”（SIRA），它将检索中的“超智能”定义为将多轮探索性搜索压缩为单次语料库判别式检索动作的能力。SIRA 不仅仅询问哪些术语与查询相关；它还询问哪些术语可能将所需的证据与语料库级别的混淆项区分开来。在语料库端，大语言模型（LLM）离线地为每个文档补充缺失的搜索词汇；在查询端，它预测查询中省略的证据词汇；并利用文档频率统计作为工具调用，以过滤掉缺失、过于常见或不太可能产生检索边际的建议术语。最终的检索步骤是结合原始查询与经过验证的扩展词的单次加权 BM25 调用。在十个 BEIR 基准测试和下游问答任务中，SIRA 取得了显著优越的性能，超越了稠密检索器和最先进的多轮智能体基准，证明了在 LLM 认知和轻量级语料库统计指导下的一个格式良好的词法查询，可以超越成本高昂得多的多轮搜索，同时保持可解释性、无需训练且高效。

## Abstract
Retrieval-augmented agents are increasingly the interface to large organizational knowledge bases, yet most still treat retrieval as a black box: they issue exploratory queries, inspect returned snippets, and iteratively reformulate until useful evidence emerges. This approach resembles how a newcomer searches an unfamiliar database rather than how an expert navigates it with strong priors about terminology and likely evidence, and results in unnecessary retrieval rounds, increased latency, and poor recall.   We introduce \textit{SuperIntelligent Retrieval Agent} (SIRA), which defines \emph{superintelligence} in retrieval as the ability to compress multi-round exploratory search into a single corpus-discriminative retrieval action. SIRA does not merely ask what terms are relevant to the query; it asks which terms are likely to separate the desired evidence from corpus-level confusers. On the corpus side, an LLM enriches each document offline with missing search vocabulary; on the query side, it predicts evidence vocabulary omitted by the query; and document-frequency statistics as a tool call to filter proposed terms that are absent, overly common, or unlikely to create retrieval margin. The final retrieval step is a single weighted BM25 call combining the original query with the validated expansion.   Across ten BEIR benchmarks and downstream question-answering tasks, SIRA achieves the significantly superior performance outperforming dense retrievers and state-of-the-art multi-round agentic baselines, demonstrating that one well-formed lexical query, guided by LLM cognition and lightweight corpus statistics, can exceed substantially more expensive multi-round search while remaining interpretable, training-free, and efficient.

---

## 论文详细总结（自动生成）

以下是对论文《Superintelligent Retrieval Agent: The Next Frontier of Information Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的检索增强型智能体（RAG Agents）通常将检索视为“黑盒”，采用类似新手的“试错法”：发出模糊查询、阅读返回片段、再迭代修改查询。这种多轮交互模式导致了高延迟、高昂的推理成本以及较低的召回率。
*   **核心问题**：如何让智能体像领域专家一样，在阅读任何检索结果之前，就能凭借先验知识和对语料库统计特性的理解，通过“单次”精准动作完成高质量检索？
*   **整体含义**：论文提出了**SIRA（超智能检索智能体）**，将检索中的“超智能”定义为将多轮探索性搜索压缩为单次语料库判别式检索动作的能力。

### 2. 方法论
SIRA 的核心思想是利用 LLM 的参数化知识来弥补查询与文档之间的词汇鸿沟，并利用语料库统计信息进行约束。
*   **离线端：语料库富化（Corpus-Side Enrichment）**：
    *   LLM 预先阅读每个文档，预测用户可能用来寻找该文档的缺失搜索词（如别名、缩写、领域术语）。
    *   使用**文档频率（DF）过滤器**：剔除在语料库中过于常见（无区分度）的词汇。
    *   将保留的术语注入 BM25 索引。
*   **在线端：查询端富化（Query-Side Enrichment）**：
    *   LLM 生成“预期响应草图”（Expected-response sketch），预测相关证据中应包含的判别性概念和实体。
    *   **DF 验证与剪枝**：通过工具调用检查建议词是否在索引中存在且具有足够的 IDF 权重（区分度）。
*   **检索执行**：
    *   将原始查询 $q_{orig}$ 与验证后的扩展词 $q_{exp}$ 结合，执行单次加权 BM25 检索：
        $score(d) = BM25(q_{orig}, d) + w \cdot BM25(q_{exp}, d)$
    *   该过程透明、可解释且无需向量数据库。

### 3. 实验设计
*   **数据集**：使用了 **BEIR 基准测试**中的 10 个数据集，涵盖问答（NQ, HotpotQA）、金融（FIQA）、事实核查（FEVER）、论点检索（ArguAna）、重复检测（Quora）等多种任务。
*   **下游任务**：在 NQ 和 HotpotQA 上进行了下游问答（QA）的答案覆盖率测试。
*   **对比方法**：
    *   **传统/神经检索**：BM25, E5 (稠密), SPLADE (稀疏), Doc2Query。
    *   **LLM 增强方法**：HyDE, CoT。
    *   **智能体基准**：Search-R1 (强化学习训练的多轮智能体), GrepRAG, ShellAgent。

### 4. 资源与算力
*   **模型使用**：实验主要使用 **Qwen3.6-35B-A3B-FP8** 作为冻结的 LLM 后端（激活参数约 3B）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但强调了该方法是 **Training-free（无需训练）** 的，且由于避免了向量索引的维护和多轮 LLM 推理，其在线推理效率极高。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同领域、规模从 5K 到 5.4M 文档不等的标准数据集上进行了测试，实验覆盖面广。
*   **客观性与公平性**：
    *   对比了包括最新的强化学习训练智能体（如 Search-R1）在内的多种前沿方法。
    *   在 QA 测试中，即使 SIRA 仅作为检索器（不带阅读器），其答案覆盖率也直接与端到端 QA 智能体的生成准确率进行对比，标准严苛。
    *   实验结果显示 SIRA 在 8/10 的数据集上达到了最优 Recall@10，证明了其稳健性。

### 6. 主要结论与发现
*   **单次胜过多轮**：一个由 LLM 认知和语料统计引导的、格式良好的单次词法查询，性能优于昂贵的多轮迭代搜索。
*   **无需训练的优越性**：SIRA 在无需任何领域内标注数据微调的情况下，平均 Recall@10 (0.691) 超过了经过大规模监督训练的稠密检索器 E5 (0.648) 和稀疏检索器 SPLADE (0.625)。
*   **统计信息的重要性**：LLM 虽然有知识，但容易产生幻觉；引入轻量级的语料库统计（DF）作为“地基”，能有效过滤无效扩展词。

### 7. 优点
*   **高效且低成本**：无需维护昂贵的向量数据库，无需 GPU 进行在线向量编码，仅需标准倒排索引。
*   **可解释性强**：基于 BM25 的关键词匹配，可以清晰追踪为什么某个文档被检索到。
*   **即插即用**：无需针对特定任务进行强化学习或微调，具有极强的通用性。

### 8. 不足与局限
*   **依赖 LLM 先验知识**：如果目标语料库包含 LLM 预训练阶段完全未见过的极端冷门领域知识，LLM 可能无法生成有效的富化术语。
*   **离线处理开销**：虽然在线检索快，但离线阶段需要 LLM 扫描整个语料库进行富化，对于超大规模动态更新的语料库，离线处理成本不容忽视。
*   **任务感知提示词**：SIRA 的提示词需要针对不同任务类型（如事实核查 vs 重复检测）进行微调，尚未实现完全自动化的提示词工程。

（完）
